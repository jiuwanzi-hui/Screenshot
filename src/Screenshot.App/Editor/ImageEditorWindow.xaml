<Window x:Class="Screenshot.App.Editor.ImageEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:editor="clr-namespace:Screenshot.App.Editor"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="编辑截图"
        Width="1120"
        Height="820"
        MinWidth="760"
        MinHeight="560"
        Background="#102027"
        FontFamily="Microsoft YaHei UI"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Geometry x:Key="UndoIconGeometry">M 8,4 L 3,9 L 8,14 M 3,9 L 11,9 C 15,9 18,11.5 18,16</Geometry>
        <Geometry x:Key="RedoIconGeometry">M 12,4 L 17,9 L 12,14 M 17,9 L 9,9 C 5,9 2,11.5 2,16</Geometry>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"
                         Color="#14272D" />
        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="38" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Padding" Value="10,3" />
            <Setter Property="Foreground" Value="#D9F5F1" />
            <Setter Property="Background" Value="#D91A3037" />
            <Setter Property="BorderBrush" Value="#5C6B9696" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F02A4B50" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#8AE1D8D0" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F0396666" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GlassTool" TargetType="RadioButton">
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Padding" Value="10,3" />
            <Setter Property="Foreground" Value="#D9F5F1" />
            <Setter Property="Background" Value="#D91A3037" />
            <Setter Property="BorderBrush" Value="#5C6B9696" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinWidth" Value="52" />
            <Setter Property="GroupName" Value="EditorTool" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="ToolBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ToolBorder" Property="Background" Value="#E62EAFA5" />
                                <Setter TargetName="ToolBorder" Property="BorderBrush" Value="#B7F1E7E0" />
                                <Setter Property="Foreground" Value="#092A2D" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToolBorder" Property="BorderBrush" Value="#8AE1D8D0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GlassSwatch" TargetType="Button">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Margin" Value="0,0,7,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderBrush" Value="#8AE1D8D0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="SwatchCircle"
                                     Fill="{TemplateBinding Background}"
                                     Stroke="{TemplateBinding BorderBrush}"
                                     StrokeThickness="{TemplateBinding BorderThickness}" />
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SwatchCircle" Property="Stroke" Value="#E8FFFC" />
                                <Setter TargetName="SwatchCircle" Property="StrokeThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="SwatchCircle" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="EditorSlider" TargetType="Slider">
            <Setter Property="Width" Value="108" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Height="3"
                                    Background="#405C7472"
                                    CornerRadius="1.5" />
                            <Track x:Name="PART_Track"
                                   Height="14"
                                   VerticalAlignment="Center">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="3"
                                                        VerticalAlignment="Center"
                                                        Background="#2EAFA5"
                                                        CornerRadius="1.5" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14"
                                           Height="14"
                                           Background="#2EAFA5"
                                           BorderBrush="#C7FFF9"
                                           BorderThickness="2"
                                           Cursor="Hand">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{TemplateBinding Background}"
                                                         Stroke="{TemplateBinding BorderBrush}"
                                                         StrokeThickness="{TemplateBinding BorderThickness}" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10"
            Background="#F212252C"
            BorderBrush="#7A5BBDB5"
            BorderThickness="1"
            CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              Direction="270"
                              Opacity="0.38"
                              ShadowDepth="8"
                              Color="#001014" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="112" />
                <RowDefinition Height="*" />
                <RowDefinition Height="34" />
            </Grid.RowDefinitions>
            <Canvas Grid.RowSpan="3"
                    IsHitTestVisible="False"
                    Opacity="0.45">
                <Ellipse Width="3" Height="3" Fill="#8AE1D8" Canvas.Left="44" Canvas.Top="108" />
                <Ellipse Width="2" Height="2" Fill="#5BB9B1" Canvas.Left="188" Canvas.Top="540" />
                <Ellipse Width="3" Height="3" Fill="#8AE1D8" Canvas.Left="760" Canvas.Top="92" />
                <Ellipse Width="2" Height="2" Fill="#5BB9B1" Canvas.Left="1010" Canvas.Top="620" />
                <Ellipse Width="2" Height="2" Fill="#8AE1D8" Canvas.Left="520" Canvas.Top="720" />
            </Canvas>

            <Border Grid.Row="0"
                    Margin="12,12,12,8"
                    Background="#D9183037"
                    BorderBrush="#3E698084"
                    BorderThickness="1"
                    CornerRadius="10">
                <Grid Margin="12,8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" LastChildFill="False">
                        <StackPanel VerticalAlignment="Center"
                                    Orientation="Horizontal">
                        <TextBlock Margin="0,0,16,0"
                                   VerticalAlignment="Center"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="#E0F4F1"
                                   Text="编辑截图" />
                        <RadioButton Tag="Rectangle"
                                     Checked="OnToolSelected"
                                     IsChecked="True"
                                     ToolTip="矩形标注"
                                     Style="{StaticResource GlassTool}">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" FontSize="16" Text="□" TextAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Text="矩形" />
                            </Grid>
                        </RadioButton>
                        <RadioButton Tag="Arrow"
                                     Checked="OnToolSelected"
                                     ToolTip="箭头标注"
                                     Style="{StaticResource GlassTool}">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" FontSize="16" Text="→" TextAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Text="箭头" />
                            </Grid>
                        </RadioButton>
                        <RadioButton Tag="Brush"
                                     Checked="OnToolSelected"
                                     ToolTip="自由画笔"
                                     Style="{StaticResource GlassTool}">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" FontSize="16" Text="✎" TextAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Text="画笔" />
                            </Grid>
                        </RadioButton>
                        <RadioButton Tag="Mosaic"
                                     Checked="OnToolSelected"
                                     ToolTip="马赛克笔刷"
                                     Style="{StaticResource GlassTool}">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI Symbol" FontSize="16" Text="▧" TextAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Text="马赛克" />
                            </Grid>
                        </RadioButton>
                        <RadioButton Tag="Text"
                                     Checked="OnToolSelected"
                                     ToolTip="文字标注"
                                     Style="{StaticResource GlassTool}">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe UI" FontWeight="SemiBold" Text="T" TextAlignment="Center" />
                                <TextBlock Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Center" Text="文字" />
                            </Grid>
                        </RadioButton>
                        <Separator Width="1"
                                   Height="24"
                                   Margin="5,0,12,0"
                                   Background="#4B829095" />
                        <Button x:Name="UndoButton"
                                Click="OnUndoClick"
                                ToolTip="撤销"
                                Style="{StaticResource GlassButton}">
                            <Viewbox Width="18" Height="18" Stretch="Uniform">
                                <Path Width="20" Height="20"
                                      Data="{StaticResource UndoIconGeometry}"
                                      Fill="Transparent"
                                      Stroke="#D9F5F1"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeThickness="2" />
                            </Viewbox>
                        </Button>
                        <Button x:Name="RedoButton"
                                Click="OnRedoClick"
                                ToolTip="重做"
                                Style="{StaticResource GlassButton}">
                            <Viewbox Width="18" Height="18" Stretch="Uniform">
                                <Path Width="20" Height="20"
                                      Data="{StaticResource RedoIconGeometry}"
                                      Fill="Transparent"
                                      Stroke="#D9F5F1"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeThickness="2" />
                            </Viewbox>
                        </Button>
                        </StackPanel>
                        <StackPanel DockPanel.Dock="Right"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                            <Button x:Name="TopmostButton"
                                    Click="OnTopmostClick"
                                    ToolTip="让编辑窗口保持在其他窗口上方"
                                    Style="{StaticResource GlassButton}">
                                <TextBlock x:Name="TopmostGlyph" FontFamily="Segoe MDL2 Assets" FontSize="14" Text="&#xE840;" />
                            </Button>
                            <Button Click="OnCopyClick"
                                    ToolTip="复制"
                                    Style="{StaticResource GlassButton}">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="14" Text="&#xE8C8;" />
                            </Button>
                            <Button Click="OnSaveClick"
                                    ToolTip="保存"
                                    Style="{StaticResource GlassButton}">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="14" Text="&#xE74E;" />
                            </Button>
                        </StackPanel>
                    </DockPanel>

                    <StackPanel Grid.Row="1"
                                Margin="0,8,0,0"
                                Orientation="Horizontal">
                        <Button x:Name="CyanColorButton"
                                Tag="#2EAFA5"
                                Click="OnColorClick"
                                Background="#2EAFA5"
                                BorderBrush="#F2FFFC"
                                BorderThickness="2"
                                ToolTip="青色"
                                Style="{StaticResource GlassSwatch}" />
                        <Button x:Name="RedColorButton"
                                Tag="#E16B71"
                                Click="OnColorClick"
                                Background="#E16B71"
                                ToolTip="红色"
                                Style="{StaticResource GlassSwatch}" />
                        <Button x:Name="LightColorButton"
                                Tag="#DDEBE8"
                                Click="OnColorClick"
                                Background="#DDEBE8"
                                ToolTip="浅色"
                                Style="{StaticResource GlassSwatch}" />
                        <Button x:Name="CustomColorButton"
                                Margin="2,0,12,0"
                                Click="OnCustomColorClick"
                                Background="#647A80"
                                ToolTip="选择自定义颜色"
                                Style="{StaticResource GlassSwatch}">
                            <TextBlock FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="#FFFFFF"
                                       Text="&#xE790;" />
                        </Button>
                        <TextBlock Margin="0,0,8,0"
                                   VerticalAlignment="Center"
                                   Foreground="#91C8C5"
                                   Text="线宽" />
                        <Slider x:Name="StrokeWidthSlider"
                                Margin="0,0,8,0"
                                Minimum="1"
                                Maximum="12"
                                SmallChange="1"
                                Style="{StaticResource EditorSlider}"
                                Value="3"
                                ValueChanged="OnStrokeWidthChanged"
                                ToolTip="线条粗细" />
                        <TextBlock x:Name="StrokeWidthText"
                                   MinWidth="34"
                                   VerticalAlignment="Center"
                                   Foreground="#BCEAE5"
                                   Text="3 px" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Margin="12,0"
                    Background="#AA0C171C"
                    BorderBrush="#3F668087"
                    BorderThickness="1"
                    CornerRadius="12"
                    ClipToBounds="True">
                <ScrollViewer x:Name="EditorScrollViewer"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalContentAlignment="Center"
                              VerticalContentAlignment="Top"
                              PreviewMouseWheel="OnEditorPreviewMouseWheel">
                    <Border x:Name="EditorViewport"
                            Background="#E61B2A30"
                            BorderBrush="#485A7C7C"
                            BorderThickness="1"
                            ClipToBounds="True">
                        <editor:ImageEditorCanvas x:Name="EditorCanvas"
                                                  Background="#E61B2A30"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Top" />
                    </Border>
                </ScrollViewer>
            </Border>

            <TextBlock x:Name="StatusText"
                       Grid.Row="2"
                       Margin="16,0"
                       VerticalAlignment="Center"
                       Foreground="#91C8C5"
                       TextTrimming="CharacterEllipsis" />
        </Grid>
    </Border>
</Window>
