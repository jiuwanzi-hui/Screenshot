# Windows 截图工具执行计划

## 文档说明

本文件是项目的唯一执行计划记录。每完成一个步骤，更新对应状态、完成日期、验证结果和阻塞项。

状态说明：

- 待开始：尚未执行。
- 进行中：当前正在执行。
- 已完成：实现和相应验证均已完成。
- 已阻塞：需要补充需求、权限或外部条件后才能继续。

## 已确定的产品约束

- 目标平台：Windows 10/11。
- 技术栈：C#、.NET 8、WPF 和 Win32 API。
- 优先目标：轻量、后台常驻、截图流程快、界面简洁。
- 程序关闭主窗口后默认继续驻留系统托盘；用户可选择是否在任务栏显示窗口图标。
- 所有应用自身数据（设置、凭据、日志和默认截图）默认保存在可执行文件同级的 `ScreenshotData` 目录；用户手动选择的截图保存位置除外。
- 通知区域图标可由用户完全隐藏；Windows 负责决定已显示的通知区域图标是否进入“隐藏图标”折叠区，应用不尝试干预该系统排序。
- OCR 优先使用本地 Windows 能力；翻译服务必须可配置，不能绑定单一服务商。

## 快捷键冲突规则

1. 本软件的快捷键由用户在设置页配置，并在保存时尝试注册。
2. 若快捷键未冲突，直接保存并生效。
3. 若快捷键被系统保留或 Windows 拒绝注册，说明原因并禁止使用该组合键，不尝试修改系统快捷键。
4. 若快捷键被第三方软件占用，提示冲突并提供处理入口。Windows 没有通用、安全的接口可以直接改写任意第三方软件的快捷键设置，因此只能在用户明确授权且目标软件具备可验证配置方式时进行适配；其他情况要求用户修改本软件快捷键或在目标软件中手动释放该快捷键。
5. 不使用键盘钩子强行拦截系统或第三方应用已注册的快捷键，以避免破坏系统行为和其他软件的输入。

## 执行计划

| 阶段 | 工作项 | 交付与验收标准 | 状态 | 完成日期 | 验证结果 / 阻塞项 |
| --- | --- | --- | --- | --- | --- |
| 0 | 产品边界与架构设计 | 确认模块边界、依赖、数据存储和高风险功能的降级策略 | 已完成 | 2026-07-11 | 已创建 docs/ARCHITECTURE.md；完成架构、数据存储、依赖控制、快捷键边界和高风险降级策略审查。 |
| 1 | 初始化工程 | 可构建、可启动的 .NET 8 WPF 工程，包含基础目录和代码规范 | 已完成 | 2026-07-11 | 已安装 .NET SDK 8.0.422；创建解决方案、WPF 应用和 xUnit 测试项目；dotnet restore、build、test 与 WPF 启动冒烟验证均通过。 |
| 2 | 后台与系统托盘 | 关闭主窗口后可常驻；托盘菜单可打开设置和退出；可配置任务栏图标显示 | 已完成 | 2026-07-11 | 已实现托盘图标、打开设置/退出菜单、关闭窗口后隐藏和显式退出；任务栏显示由窗口接口控制，默认隐藏。构建 0 警告，3/3 测试通过，后台无任务栏窗口启动验证通过。 |
| 3 | 设置中心与配置持久化 | 常规设置、保存路径、托盘/任务栏显示、开机自启和配置文件可正常保存恢复 | 已完成 | 2026-07-11 | 已实现常规设置页、保存目录选择、任务栏图标开关和开机启动开关；配置、凭据和默认截图统一写入可执行文件同级 `ScreenshotData`，启动项使用当前用户注册表。构建 0 警告，相关测试通过。 |
| 4 | 全局快捷键 | 可配置截图、长截图、OCR、贴图和设置快捷键；按上述冲突规则处理 | 已完成 | 2026-07-11 | 已实现可编辑的 5 组全局快捷键、格式解析、重复检测、已知系统保留键拒绝和 RegisterHotKey 原子注册/回滚；第三方占用时明确提示用户释放或换键，不强行拦截。构建 0 警告，12/12 测试通过，罕见组合键真实注册/释放验证通过。 |
| 5 | 基础区域截图 | 多显示器透明遮罩、框选、Esc 取消、截图预览均可用 | 已完成 | 2026-07-11 | 已实现基于虚拟桌面的透明框选遮罩、Esc 取消、物理像素区域换算、实际屏幕抓取和预览窗口；应用声明 PerMonitorV2 DPI 感知。构建 0 警告，15/15 测试通过，真实屏幕像素抓取和预览位图验证通过。 |
| 6 | 截图结果处理 | 支持复制到剪贴板、保存到指定路径、重新框选和基础历史记录 | 已完成 | 2026-07-11 | 已在预览中实现复制到系统剪贴板、保存 PNG、重新框选；保存采用 CreateNew 防止覆盖同名文件。新增可选的会话内缩略图历史和托盘入口。构建 0 警告，17/17 测试通过，真实 PNG 保存和历史容量验证通过。 |
| 7 | 图片编辑器 | 支持文字、箭头、矩形、画笔、马赛克、撤销和重做 | 已完成 | 2026-07-11 | 已实现独立编辑器：矩形、箭头、画笔、文字输入、马赛克、颜色/线宽、撤销/重做，以及编辑结果复制和 PNG 保存。编辑注释以命令模型管理，原图不被破坏。构建 0 警告，20/20 测试通过，马赛克、命令栈和编辑器窗口验证通过。 |
| 8 | 钉图 | 截图可作为无边框置顶图窗显示，支持拖动、缩放、透明度、复制和关闭 | 已完成 | 2026-07-11 | 已实现独立无边框置顶钉图窗口，可拖动、右下角缩放、调节透明度、复制和关闭；钉图不显示任务栏图标，管理器在应用退出时统一释放。构建 0 警告，22/22 测试通过，置顶和无任务栏属性验证通过。 |
| 9 | OCR 识别 | 可识别截图或选区文字，支持复制结果和识别语言设置 | 已完成 | 2026-07-11 | 已接入 Windows 本地 OCR：支持从截图预览或独立选区识别，结果窗口可编辑和复制；设置页可配置 OCR 语言标记，并对缺失语言包给出明确提示。项目目标为 net8.0-windows10.0.19041.0。构建 0 警告，25/25 测试通过，语言枚举和缺失语言包降级验证通过。 |
| 10 | 翻译 | 可翻译 OCR 文本；支持翻译服务配置、网络失败提示和结果复制 | 已完成 | 2026-07-11 | 已实现 OCR 结果的显式翻译按钮和 OpenAI 兼容 HTTPS 接口配置。默认不发送任何文字；需用户开启在线发送并点击翻译。API Key 通过当前用户 DPAPI 加密保存，不进入 settings.json。已处理无配置、无凭据、HTTP、鉴权、网络和响应格式错误。构建 0 警告，29/29 测试通过，所有协议测试使用本地模拟响应。 |
| 11 | 长截图 | 覆盖浏览器和常规可滚动窗口；支持拼接、取消和失败降级 | 进行中 |  | 已实现框选滚动区域后恢复原窗口焦点，由用户滚动鼠标；程序以 40ms 间隔采样，最后一个滚轮事件后继续采样 360ms，覆盖浏览器平滑滚动尾段。拼接器以压缩的纹理视口锚点和双向位移匹配定位当前帧：反向滚回已采集范围只刷新锚点，越过顶部或底部才添加条带，即使滚轮方向滞后也不会漏掉上方内容。匹配固定中央内容带并忽略常见顶部栏；比较时以文字、头像和图片等高纹理区域加权，纯色背景不再主导置信度。待拼接帧还会与最近 4 个已确认视口交叉验证，逻辑位移矛盾即拒绝，默认置信度提高至 0.96，避免聊天记录等稀疏布局的重复和缺段。滚动期间保留点击穿透的选区边框，右侧实时预览持续更新且两者在取帧时暂时隐藏，避免进入结果图。Release 长截图专项测试 14/14、框选遮罩专项测试 7/7、全量测试 69/69 通过；等待本轮 Release 的浏览器和实际软件人工验收。 |
| 12 | 开机启动与系统集成 | 可开启/关闭开机启动，状态与 Windows 启动项实际状态一致 | 已完成 | 2026-07-11 | 已使用当前用户 `Run` 注册表项启用/关闭开机启动；启动命令带 `--background`，开机时静默驻留，手动启动仍显示设置窗口。启动项测试 2/2、全量测试 50/50 通过。 |
| 13 | 性能与稳定性 | 验证后台内存、资源释放、热键恢复、异常日志和多显示器兼容性 | 待开始 |  |  |
| 14 | 界面打磨与发布 | 完成简洁界面、深浅色支持、安装包、版本信息和使用说明 | 进行中 |  | 已完成 1.0.2 自包含 x64 安装包与免安装包、动态深浅色中文安装向导、自定义安装位置、版本信息与中英文 README；待后续正式发布签名。 |
| 15 | 微信/PixPin 工作流重构 | 设置即时生效、按键录入快捷键、便携数据目录、可隐藏通知区图标，以及框选后留在屏幕内调整和编辑 | 进行中 |  | 15.1 至 15.3 已完成；15.3 已补充右键逐层返回；15.4 已完成本轮边界去重、反向回滚去重、动态视口去重、方向滞后补偿、选区常显和右侧实时预览的实现与自动验证，等待用户用本轮 Release 完成实际滚动页面验收。 |
| 16 | 跨平台核心与 macOS 版 | 拼接内核抽为平台无关库并有独立测试；macOS 端可抓屏、监听滚轮并产出滚动长截图；最终提供菜单栏 App | 进行中 |  | 16.1 SnapCut.Core 与 16.2 测试已完成（53/53）；16.3 macOS CLI 原型已完成编译与 osx-arm64 交叉发布，待在真实 Mac 上人工验收；Windows 端代码全程未改动。 |

### 阶段 15 分项

| 分项 | 工作项 | 状态 | 验收标准 / 验证结果 |
| --- | --- | --- | --- |
| 15.1 | 便携存储与通知区图标 | 已完成 | 设置、凭据和默认截图已改为可执行文件同级 `ScreenshotData`；新增“显示通知区域图标”开关，关闭后完全隐藏该图标。Release 构建 0 警告、0 错误；4/4 存储与设置测试通过。Windows 不允许应用指定图标位于主托盘还是隐藏图标折叠区。 |
| 15.2 | 即时设置与快捷键录入 | 已完成 | 已移除手动保存按钮；勾选项立即生效，文本项停止输入 400ms 或离焦后生效，快捷键框直接捕获键盘组合并即时注册。每项快捷键可通过清除按钮或 `Backspace/Delete` 留空，清空后立即注销对应全局热键；无效、系统保留或冲突组合会回滚并显示原因。Release 专项测试 16/16 通过。 |
| 15.3 | 微信式框选后流程 | 已完成 | 框选后保留屏幕遮罩与选区，支持拖动和八方向缩放；点击编辑后在原选区冻结画面并直接使用矩形、箭头、画笔、文字、马赛克、颜色、线宽、撤销和重做，不再打开独立编辑窗口。鼠标右键按层级处理：取消正在绘制／输入的标注、退出编辑回到选区、清除选区，最后取消截图。工具栏对号会复制当前编辑结果并关闭遮罩，同时保留保存、OCR、钉图和取消。编辑与遮罩专项测试 10/10 通过。 |
| 15.4 | PixPin 式长截图 | 已完成 | 主流程为受控双向状态机：等待开始时单击可先向下、双击可先向上；固定计时器优先注入每次 2px 的触摸拖动，使页面以约 40px/s 连续移动，目标不支持触摸时才降级为每 700ms 一个完整滚轮刻度。采样与拼接耗时不再改变可见滚动速度。单击暂停或滚动中直接完成都会先停止输入、持续补采到画面稳定，避免两端缺行；第一方向内双击停稳后快速返回初始视口，随后启用相反方向的独立拼接链。上下两段以共同初始帧确定性合并。物理滚轮被拦截。选区右侧实时显示完整结果和阶段提示；右键仍可一次取消。 |

### 阶段 16 分项

| 分项 | 工作项 | 状态 | 验收标准 / 验证结果 |
| --- | --- | --- | --- |
| 16.1 | SnapCut.Core 平台无关拼接内核 | 已完成 | `src/SnapCut.Core`（net8.0，零第三方依赖）：`PixelImage` BGRA 光栅 + `ImageOverlapMatcher`（与 Windows 版逐行同源，仅 Bitmap→PixelImage）+ `ScrollCaptureComposer`、`ViewportFingerprint`、`CapturedFrameGate`、`ScrollWheelMotionTracker`、`ScrollFrameSelection`、方向/选项类型。GDI+ 绘制全部换成确定性的字节位块传送与盒式缩放。Release 构建 0 警告 0 错误。 |
| 16.2 | 核心库独立测试 | 已完成 | `tests/SnapCut.Core.Tests`（net8.0，可在任意平台运行）：移植拼接器全套 26 用例与快速滚动 13 用例（逐像素校验、含反向甩动/混合速度/水平微对齐/周期内容），新增 PixelImage 与帧闸门专项。Release 53/53 通过。 |
| 16.3 | macOS CLI 原型 | 进行中 | `src/SnapCut.Mac`（net8.0，纯 C-ABI P/Invoke：CoreGraphics/CoreFoundation/ImageIO/CGEventTap，无 ObjC 依赖，Windows 可交叉编译）：显示器枚举、区域抓屏→BGRA 归一化、ImageIO 存 PNG、听式滚轮 EventTap、滚动采集引擎（链式顺序拼接＋采样背压＋抽帧位移合并＋CarriedMotion，与 Windows 采样规则同源）；命令 `displays/permissions/capture/scroll`。Windows 上构建 0 警告 0 错误，osx-arm64 自包含发布产出验证通过。待在真实 Mac 上验收权限流程、抓屏与实际滚动拼接。 |
| 16.4 | 菜单栏 App（框选、预览、快捷键） | 待开始 | 需 ObjC 互操作（NSStatusItem、遮罩窗、热键）或迁移 ScreenCaptureKit；在 CLI 原型验收后设计。 |
| 16.5 | Windows 端切换复用 SnapCut.Core | 待开始 | 消除匹配器/拼接器双份副本（当前 Windows 端保持原实现不动，算法修改需双写；切换涉及 Bitmap↔PixelImage 边界与预览路径，需全量回归后再做，等待用户确认时机）。 |

## 高风险功能与策略

| 功能 | 风险 | 首版策略 |
| --- | --- | --- |
| 长截图 | 应用程序的滚动机制差异大，自动拼接容易错位 | 先支持浏览器和常规滚动窗口；不能可靠识别时提示降级为普通截图 |
| 第三方快捷键冲突 | Windows 不能识别并通用修改占用方的配置 | 只检测并提示；仅对明确支持的应用提供用户授权后的专门适配 |
| OCR 与翻译 | 离线能力、语言包和网络服务可用性不同 | OCR 优先本地；翻译服务可配置并提供清晰的失败状态 |
| 后台常驻 | 资源泄漏会长期影响系统 | 统一管理托盘、截图资源、置顶窗口和全局快捷键的生命周期 |

## 更新记录

| 日期 | 更新内容 | 执行人 |
| --- | --- | --- |
| 2026-07-11 | 创建初始计划；补充快捷键冲突和系统保留快捷键处理规则 | Codex |
| 2026-07-11 | 完成阶段 0：写入架构设计并完成文档验收 | Codex |
| 2026-07-11 | 完成阶段 1：初始化 .NET 8 WPF 工程、测试工程和构建规范；构建、测试、启动验证通过 | Codex |
| 2026-07-11 | 完成阶段 2：实现后台常驻、系统托盘和窗口隐藏生命周期；构建、测试和后台启动验证通过 | Codex |
| 2026-07-11 | 完成阶段 3：实现设置中心、配置持久化和当前用户开机启动；构建和 7 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 4：实现全局快捷键配置、系统保留键处理和 Windows 注册验证；构建和 12 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 5：实现区域截图、虚拟桌面坐标换算和截图预览；构建和 15 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 6：实现截图复制、保存、重选和会话内历史；构建和 17 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 7：实现图片编辑器、马赛克和撤销/重做；构建和 20 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 8：实现钉图置顶窗口和生命周期管理；构建和 22 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 9：实现本地 Windows OCR、结果复制和语言设置；构建和 25 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 10：实现可选在线翻译、DPAPI 凭据保护和网络失败处理；构建和 29 项功能测试通过 | Codex |
| 2026-07-11 | 开始阶段 11：长截图进入实现与验证，计划表状态更新为进行中 | Codex |
| 2026-07-11 | 根据交互反馈新增阶段 15：微信/PixPin 工作流重构；15.1 便携存储与通知区图标开始实现 | Codex |
| 2026-07-11 | 完成阶段 15.1：便携数据目录和通知区域图标开关；Release 构建及 4 项相关测试通过 | Codex |
| 2026-07-11 | 完成阶段 15.2：即时设置、键盘快捷键录入、冲突回滚与设置窗口验证；Release 构建及 12 项相关测试通过 | Codex |
| 2026-07-11 | 完成阶段 15.3：微信式框选后调整、工具条与操作流程；Release 构建、2 项选区窗口测试及 41 项非长截图回归测试通过 | Codex |
| 2026-07-11 | 完成阶段 12：开机启动使用 `--background` 静默驻留；启动项测试 2/2、全量测试 50/50 通过 | Codex |
| 2026-07-11 | 阶段 11/15.4 实现和自动验证完成：长截图改为框选后由用户滚动、点击完成再拼接；专项测试 6/6、Release 全量测试 50/50 通过，进入实际软件人工验收 | Codex |
| 2026-07-11 | 补充阶段 15.2：允许任意快捷键留空并立即注销对应注册，增加清除按钮和 `Backspace/Delete` 操作；专项测试 16/16 通过 | Codex |
| 2026-07-11 | 重做阶段 11/15.4 长截图匹配：恢复目标窗口焦点、提高采样频率、容忍固定顶部和滚动条、后台匹配并改为低内存条带存储；专项测试 8/8 通过 | Codex |
| 2026-07-11 | 重做阶段 15.3 编辑流程：编辑画布嵌入原截图选区，支持原有五类工具与撤销/重做；增加对号复制并关闭。专项测试 8/8，实际界面框选、绘制和关闭验证通过 | Codex |
| 2026-07-12 | 修复阶段 11/15.4：在重叠匹配前拒绝到顶/到底的零位移帧，避免周期性内容重复拼接；长截图时保留点击穿透的选区边框，并在右侧持续显示实时预览，取帧时暂隐二者以确保输出不受 UI 污染。Release 长截图专项测试 10/10、框选遮罩专项测试 5/5、全量测试 61/61 通过；Release 构建 0 警告、0 错误。桌面交互测试已串行化，避免全局滚轮、前台窗口和剪贴板互相干扰。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 反向回滚重复拼接：拼接器改为记录当前视口和已采集范围的逻辑坐标；视口回到既有范围只更新锚帧，越过顶部或底部才保存新增条带。新增“下滑后上滑回原区域再继续下滑”回归测试；将 WPF 集成测试改为注入滚轮事件，避免受用户桌面前台窗口影响。隔离 Release 构建 0 警告、0 错误；长截图专项测试 11/11、框选遮罩专项测试 5/5、全量测试 62/62 通过。 | Codex |
| 2026-07-12 | 加固阶段 11/15.4 重复检测：针对卡片列表等周期性布局在末端因悬停、动画或滚动条轻微变化而误判新条带的问题，新增压缩视口指纹历史；命中既有视口时恢复该位置且不拼接。新增“轻微动态变化的重复末端帧”回归测试。隔离 Release 构建 0 警告、0 错误；长截图专项测试 12/12、框选遮罩专项测试 5/5、全量测试 63/63 通过。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 反向滚动漏段与接缝错位：实现 48×36 纹理视口锚点、双向图像位移校验和方向滞后补偿；将匹配区域固定为排除常见顶部栏的中央内容带，避免卡片网格的局部重复误导整帧；最后一个滚轮事件后继续采样 360ms，覆盖平滑滚动尾段。新增“方向滞后仍向上补图”“小范围动态变化后回滚”和“下→上→下逐像素连续”回归测试。Release 长截图专项测试 13/13、框选遮罩专项测试 5/5、全量测试 66/66 通过。 | Codex |
| 2026-07-12 | 补充阶段 15.3 鼠标右键逐层返回：预备状态直接取消；有选区时清除选区；编辑时先取消未完成的绘制或文字输入，再退出编辑回到选区。新增“选区→取消”和“编辑→选区→取消”遮罩回归测试。Release 框选遮罩专项测试 7/7、全量测试 68/68 通过。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 稀疏聊天记录误拼：重叠比较改为按局部纹理加权，文字、头像和图片主导位移判断，纯色背景不再提高错误候选的置信度；待拼接帧与最近 4 个已确认视口交叉验证，坐标不一致即拒绝；默认重叠置信度提高至 0.96。新增“稀疏聊天气泡、动态消息、逐像素连续”回归测试。Release 长截图专项测试 14/14、框选遮罩专项测试 7/7、全量测试 69/69 通过。 | Codex |
| 2026-07-13 | 修复阶段 11/15.4 重复拼接与预览条纹：恢复左侧行号/头像锚点，按窗口宽度提高横向采样密度并保留窄窗口滚动条容错；拼接预览改为先完成整图再单次缩放；新增重复代码行号对齐和一次性预览缩放回归测试。Release 长截图专项测试 16/16、框选遮罩专项测试 7/7、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 完成长截图结果交互：滚动完成后的预览定位在框选区域右侧并锁定选区；新增对号按钮，点击后复制图片并关闭预览；编辑按钮进入详情编辑器，保留保存、钉图和 OCR 操作。Release 全量测试 71/71、构建 0 警告 0 错误。 | Codex |
| 2026-07-13 | 更新应用视觉标识：新增高对比截图取景框图标，扩大主体占比以适配任务栏小尺寸；接入 EXE、WPF 窗口和系统托盘。Release 构建 0 警告 0 错误。 | Codex |
| 2026-07-13 | 修复长截图缺段与交互状态：重叠候选改为采用置信度最高的真实位移，不再偏向较小位移；采样间隔缩短至 20ms，默认接受帧上限提高至 600，达到上限后不再自动结束而是等待用户操作。框选完成后先保留移动和八方向缩放，点击确认开始滚动后才锁定；右侧实时预览直接提供对号和编辑，对号生成后复制并关闭，编辑生成后进入详情编辑器。Release 长截图专项 16/16、框选遮罩专项 7/7、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 重构长截图首帧交互：框选完成即显示右侧首帧预览，移动和八方向缩放会同步更新选区、滚轮监听区域与预览；第一个滚轮消息分发前同步锁定选区并切换点击穿透，底层窗口从第一下开始滚动。普通截图在遮罩显示前冻结屏幕，编辑直接裁剪冻结帧，消除隐藏/恢复遮罩的闪屏；编辑画布图片改为填充像素画布，修复高 DPI 下二次缩放形成的小块。Release 相关专项 26/26、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复遮罩与边界滚动：滚轮前保留手柄可调和遮罩拦截，滚轮后仅选区内部点击穿透，遮罩外继续阻止其它应用操作；移除选区旁重复取消按钮。新增白色空白帧过滤，避免滚动到底后继续滚动拼接白块；保留视口指纹和双向坐标校验，降低下滚到底再上滚时的重复/错位。Release 相关专项 23/23、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复翻译 API Key 输入后消失：保存设置后保留 PasswordBox 的掩码内容，并跟踪 Key 是否被修改；非空 Key 加密保存，清空输入时删除对应凭据，不再因自动保存清空界面。Release 全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 完善 OCR 与大模型翻译：OCR 输入在尺寸上限内自动放大至最多 2 倍，改善小字号截图识别；新增可配置翻译模型，DeepSeek 域名自动将默认模型映射为 `deepseek-chat`，兼容其它 OpenAI 格式模型。翻译错误继续显示 HTTP 状态便于定位。Release 翻译/OCR 专项 6/6、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复大模型翻译配置持久化和 404：启动时从 DPAPI 凭据库重新加载 API Key 并显示掩码；设置页正确显示模型名称；DeepSeek/OpenAI 根地址自动补全聊天完成接口路径；OCR 结果窗口点击翻译时读取最新设置，不再保留配置前的旧快照。新增 DeepSeek 根地址与 `deepseek-chat` 请求回归测试。Release 翻译/OCR/设置专项 8/8、全量测试 72/72 通过。 | Codex |
| 2026-07-13 | 修复长图编辑白边和滚动闪烁：详情编辑器图片改为填满原始像素画布，消除高 DPI 下上下大块留白；采集帧时只隐藏选区边框，四周遮罩持续显示，不再逐帧闪烁。第一次滚动后拦截鼠标点击、侧键、水平滚轮和全部键盘输入，只允许选区内垂直滚动及右侧预览操作，避免切换应用或修改后方内容。Release 相关专项 26/26、全量测试 72/72 通过。 | Codex |
| 2026-07-13 | 紧急修复长截图输入锁死：移除全局键盘封锁，避免系统快捷键和其它窗口全部失效；鼠标限制增加右侧预览白名单，取消、编辑、对号和窗口关闭始终可点击，退出长截图后立即卸载钩子。Release 相关专项 25/25、全量测试 72/72 通过。 | Codex |
| 2026-07-15 | 根据 EV 操作录像与微信/本软件结果图修复长截图：滚动后改用绘制在选区外侧的独立轮廓，采集时不再隐藏/恢复边框，彻底消除框闪烁；视口指纹恢复左边缘行号/头像并缩小右侧忽略区，粗匹配增加横向采样和候选数；零位移边界置信差从 0.025 收紧至 0.006，避免真实滚动被判为静止。测试改用真实编辑器式固定深色边栏，覆盖滚到底再回滚不重复中间代码行。Release 长截图/遮罩专项 25/25、全量测试 80/80 通过。 | Codex |
| 2026-07-15 | 加固重复代码拼接与遮罩可见性：匹配增加左侧稀疏锚点独立证据，主体负责大致位移，行号/头像负责消除重复内容歧义；只有左侧纹理明显稀疏时启用加权，避免影响浏览器随机内容和固定头部。新增非整行滚动距离的下→上→下连续性测试。四周遮罩提高不透明度，滚动后外置轮廓加粗且全程保持显示。Release 长截图专项 18/18、长截图/遮罩专项 26/26、全量测试 81/81 通过。 | Codex |
| 2026-07-14 | 修复翻译/DeepSeek API Key 不生效：翻译服务类型不再依赖用户手填魔法字符串，空值或大小写不一致统一解析为唯一支持的 OpenAI 兼容类型；保存/读取凭据改用解析后的稳定 provider id，不再因服务类型为空触发异常导致 Key 存不进去。`TranslationProvider` 默认值改为 `OpenAICompatible`，设置页提示补充 DeepSeek 地址与模型示例。新增空/异常大小写解析与凭据键回归测试。 | Claude |
| 2026-07-14 | 补充微信/PixPin 式区域截图交互：选区内双击直接确认（复制并完成），与 Enter、工具栏对号共用同一确认逻辑；OCR 结果窗口在已开启在线翻译且识别出文字时自动翻译一次，结果窗口仍保留供手动操作。Release 构建 0 警告 0 错误，全量测试 79/79 通过。 | Claude |
| 2026-07-15 | 防止长截图遮罩再次造成全局鼠标锁死：低级鼠标钩子仅在长截图选区内部拦截点击、侧键和水平滚轮，选区外交由全屏遮罩处理；右侧预览仍保留点击白名单，键盘输入不做全局拦截，钩子在完成、取消、异常和窗口关闭后释放。新增鼠标消息分类回归测试。Release 遮罩/输入专项 37/37、全量测试 92/92 通过。 | Codex |
| 2026-07-15 | 修复滚动长截图界面缺失：发布选区时立即计算四周遮罩和滚动轮廓尺寸，避免锁定后出现 0×0 的不可见框选区域；右侧实时预览窗口在显示和选区变化后显式提升到遮罩之上，同时保持不激活、不抢目标窗口焦点。新增轮廓尺寸回归断言。 | Codex |
| 2026-07-15 | 根据《本软件长截图视频》修复右键与长截图状态返回：选区发布后第一次右键清空选区并允许重新框选，第二次右键取消整个截图；滚动锁定后选区内右键由低级鼠标钩子转为专用取消信号并吞掉，避免触发后方应用，选区外遮罩和右侧预览上的右键也能取消。取消后统一释放滚轮钩子、关闭预览和遮罩。新增逐级右键回归测试；Release 全量测试 93/93 通过。 | Codex |
| 2026-07-15 | 消除第一下滚轮后的输入竞态：滚轮钩子检测到首个有效滚轮消息时立即开启非滚轮鼠标拦截，保证从滚动开始右键取消不会穿透到目标窗口。Release 长截图/遮罩专项 38/38、全量测试 93/93、构建 0 警告 0 错误。 | Codex |
| 2026-07-15 | 根据 EV 录屏再次修复长截图可见性：移除遮罩与右侧预览窗口的永久 `WDA_EXCLUDEFROMCAPTURE`，改为仅在 `CopyFromScreen` 读取目标帧的瞬间启用捕获排除，读取后立即恢复并重新提升预览层级；删除每帧 16ms 的排除等待，避免录屏中持续消失或形成闪烁。滚动轮廓提升绘制层级并加粗，新增取帧排除/恢复成对回归断言。Release 相关专项 21/21、全量测试 93/93 通过。 | Codex |
| 2026-07-15 | 修正上一版逐帧切换造成的强烈闪烁：彻底移除滚动采样循环中的显示亲和性切换与重复置顶，遮罩、外置边框和预览在滚动期间不再改变窗口合成状态；边框完全绘制在选区外，预览强制位于选区外，保持输出干净。预览尺寸按实际 DPI 换算，并按选区所在显示器工作区定位：优先紧贴右侧，右侧剩余 200–299px 时自动缩窄，连操作按钮都放不下时才紧贴左侧，不再退到屏幕左上角。新增自适应宽度与左右侧贴边定位测试。Release 全量测试 96/96、构建 0 警告 0 错误。 | Codex |
| 2026-07-20 | 滚动拼接升级为微信式多阶段匹配：行剖面粗相关覆盖全部位移、密集像素复核峰值、连续滚动时间先验抑制周期内容误匹配、近置信度并列优先更小连续步长；采样 16ms 主频 + 滚轮静止后 24ms 稳定帧、惯性尾窗 420ms。新增周期内容时间先验与多小步连续滚动无缝拼接回归。Release 全量测试 98/98 通过。 | Codex |
| 2026-07-20 | 继续逼近微信长截图：自适应时间先验邻域（8–64px，随上次步长放大）、峰值±3 局部精修、弱峰值过渡帧丢弃；下行拼接增量复合缓存 + 预览直缩放避免每帧整图克隆；新增大步跳变与微信式气泡会话拼接回归。Release 长截图专项 38/38、全量 100/100 通过。 | Codex |
| 2026-07-20 | 补全条带模板匹配与水平微对齐；修复粘性页头场景下 full/lower 频带混合把正确匹配置信度压到 0.99 以下的问题；密集阶段跳过条带证据、时间先验强时跳过远距 lattice 以保跟手；成功对齐即更新位移先验。Release 全量 100/100。 | Codex |
| 2026-07-23 | 新增关闭按钮最小化/退出设置；完成 1.0.0 自包含 x64 安装包，支持动态深浅色中文向导、自定义安装位置、当前用户/所有用户安装、快捷方式和标准卸载。补充中英文 README 与隐私排除规则。Release 全量测试 131/131，安装、启动、卸载验证通过。 | Codex |
| 2026-07-23 | 1.0.2 将安装版和免安装版的数据位置统一为程序目录下的 `ScreenshotData`；安装器创建可写数据目录，安装版首次启动可迁移 1.0.0 的 `%LocalAppData%\Screenshot`，成功后删除旧目录，冲突时不覆盖并提示用户；长截图诊断也改为写入 `ScreenshotData\Diagnostics`。卸载时清理程序文件、快捷方式、注册信息和开机启动项，并由用户选择是否连同 `ScreenshotData` 和当前用户旧版数据一起删除。Release 全量测试 133/133。 | Codex |
| 2026-07-24 | 设置窗口支持拖动缩放；应用限制为当前会话单实例，重复启动会唤起已有窗口；普通截图框选后自动显示截图内容和内嵌编辑工具；工具栏按功能分组为同一行，使用留白更宽、颜色更亮的竖线分隔，颜色和粗细放在第二行；真正删除普通截图“编辑”按钮和旧状态返回，选区在无标注时可移动缩放；对号复制改用带重试的原生剪贴板写入；长截图改为普通框选工具栏入口并复用当前选区；OCR 增加截图内可选择文字层；新增 12 种由程序绘制的彩色表情贴纸，并扩大撤销/重做图标的可用绘制区域。独立输出目录 Release 全量测试 142/142。 | Codex |
| 2026-07-24 | 普通截图工具栏新增直接翻译按钮：复用当前 OCR 与翻译设置，一次请求批量翻译识别行并按原文字区域覆盖到图片；复制、保存会包含译文，整组译文可一次撤销；未启用在线发送、配置不完整、识别或接口失败时保留截图并显示明确原因。Release 全量测试 145/145。 | Codex |
| 2026-07-24 | OCR 语言、翻译服务、目标语言和模型改为统一主题的下拉选择，其中模型仍允许自定义输入；OCR 下拉读取 Windows 已安装语言包。根据 DeepSeek 2026-07-24 的 V4 API 变更，旧模型值 `DeepSeek`、`deepseek-chat`、`deepseek-reasoner` 自动归一为 `deepseek-v4-flash`，下拉同时提供 `deepseek-v4-pro`；HTTP 失败提示加入服务端错误正文。普通截图最后一步右键改为在遮罩内完整消费按下与抬起消息后再关闭，避免底层软件收到右键。Release 全量测试 149/149。 | Codex |
| 2026-07-24 | 翻译配置扩展为通用 OpenAI 兼容厂家：服务地址可填写基础 `/v1`（包括多级路径下的 `/v1`）或完整 Chat Completions 地址，程序统一解析翻译与 `/models` 端点；新增“获取模型”按钮，使用当前 API Key 获取并解析标准 `data[].id` 及兼容 `models` 列表，动态更新可编辑模型下拉框。厂家未开放模型列表时显示服务端原因并保留手动模型输入。Release 全量测试 156/156。 | Codex |
| 2026-07-24 | 修复英文被当作中文原文导致“翻译成功但内容不变”：截图覆盖翻译和独立 OCR 翻译窗口统一改为自动检测原文语言，不再复用 OCR 识别语言；提示词明确要求翻译普通语句并保留 URL、标识符与错误码。若所有分段译文与原文实质相同，则不覆盖截图并提示模型原样返回。Release 全量测试 158/158。 | Codex |
| 2026-07-24 | 翻译覆盖层按每个原文区域采样底图颜色：深色底使用近似深色背景和浅色文字，浅色底使用浅色背景和深色文字；取消整行 Viewbox 强制缩小，字号提高并支持必要时换行。普通截图预备状态新增窗口吸附，按顶层窗口 Z 序枚举鼠标下方的可见非最小化窗口，过滤桌面和隐藏窗口，多显示器下使用物理像素/DPI 坐标换算；悬停显示边界，单击直接选取，拖动超过系统阈值则切回自由框选。Release 全量测试 159/159。 | Codex |
| 2026-07-24 | 修复吸附停留在后方全屏窗口：不再依赖可能丢失最后位置的 MouseMove 限频，遮罩每 50ms 读取真实光标位置并按 Z 序刷新，开始选区后立即停止；新增重叠窗口优先前景窗口测试。OCR/译文可选择层提升到编辑边框上方，选中文字的 Ctrl+C 改为带重试的原生 CF_UNICODETEXT 写入并提示 Win+V；翻译完成后直接显示可选择译文层，独立 OCR 窗口增加“复制译文”。翻译状态拆分本机 OCR 与在线请求并显示在线耗时。Release 全量测试 160/160。 | Codex |
| 2026-07-24 | OCR 全局快捷键不再走“轻量框选→独立结果窗口”旧流程，改为启动普通截图编辑遮罩并带自动识别标记；窗口吸附或自由框选完成后自动执行本地 OCR，在原截图上显示可选择文字层，同时保留翻译、保存、钉图、长截图和标注工具。Release 全量测试 161/161。 | Codex |
| 2026-07-24 | 发布版本提升至 1.1.0；完成自包含 win-x64 安装版与免安装版打包。安装包 52,284,891 字节，SHA-256 `A45A9CFC75C7BC17B9B3A887991381A8DF4A80B43296B1237D7E78B54D6CB85C`；免安装包 71,992,733 字节，SHA-256 `2CF864A758AB550F363C62EE6A3C814B52F2986599F74D2A71CF47DBE499B894`。发布前 Release 全量测试 161/161，通过 ZIP 内容、版本号、忽略规则及个人配置扫描。 | Codex |
| 2026-07-24 | 发布 1.1.1 补丁版本：修复完成框选后点击框外导致图片移动和工具栏消失；窗口吸附过滤 NVIDIA GeForce Overlay 等透明全屏覆盖层；新用户及缺少对应字段的旧配置默认同时显示任务栏和通知区域图标。安装包 52,275,268 字节，SHA-256 `E53B33CC6E02EE57E5300CB0EAC4B6A713D946DFD8347B0D44C99965C1AEBBE8`；免安装包 71,992,174 字节，SHA-256 `E4550DFA87439CBF2C3ACDFB4E49882C3861D19582FD3B1FC69BDD2E5066FDB7`。Release 全量测试 166/166，通过 ZIP 内容、版本号和个人配置检查。 | Codex |
| 2026-07-24 | 发布 1.2.0：快捷键按下阶段冻结桌面以保留右键菜单、下拉框和悬浮提示；截图历史支持查看与复制；OCR/翻译直接覆盖到当前截图且文字可选择复制；选区移动使用固定底图和明暗遮罩，编辑后缩放受标注边界保护；深色托盘菜单完全接管悬停绘制。安装包 52,307,144 字节，SHA-256 `1E5604C8ABA5C7F40C52893B0CF209D5E3CBE463B89FF43570BEFE53ECDD4B23`；免安装包 72,006,561 字节，SHA-256 `62EF2794F57A129D58428858E6DA751850E319CE0C19AC8A23744AB9F6698879`。Release 全量测试 173/173，安装/卸载、ZIP 内容、版本号、忽略规则和个人配置检查通过。 | Codex |
| 2026-07-24 | 发布 1.3.0，左侧新增独立“版本更新”页面：显示当前版本及安装/免安装类型，从 GitHub latest Release 的固定 `Screenshot-Update.json` 清单检查新版本；旧 Release 缺少清单时回退读取 GitHub latest 跳转，当前版本不再显示 404。下载时校验声明大小和 SHA-256；安装版静默覆盖原目录并重启，免安装版由新版程序副本等待旧进程退出后安全替换并拦截 ZIP 路径穿越；两种更新均保留 `ScreenshotData` 并清理下载包/更新进程。安装包 52,312,603 字节，SHA-256 `19B44670FAF8B1F23B45FE4113115A79518C95DEC20EB6DE7D78CA4704E46C8D`；免安装包 72,015,826 字节，SHA-256 `4DF2679C6E0FAE6E4E4265805A6945F78FA52C8A0570C861D939FA84098BBAD2`。Release 全量测试 180/180，实际 UI、清单、ZIP、安装/卸载和个人配置排除验证通过。 | Codex |
| 2026-07-24 | 发布 1.3.1：更新检查并发探测 Gitee 国内源与 GitHub 国际源，采用先返回的有效清单，更新包下载失败时自动切换备用源；更新清单同时提供两站下载地址，并在仓库 `updates/` 保存 Gitee 固定入口。README 增加 Release、下载量、Issues、Stars、Windows、.NET 与 Gitee 镜像徽章及双站下载链接。安装包 52,332,239 字节，SHA-256 `C1E52B8772412A1C28628A7821ABBEC0B424A32328F811B359140DAC96F0DD79`；免安装包 72,018,694 字节，SHA-256 `478F033086C1CA49B0397D990BDEB6EDC78DE99B2F99C8169D563E11F21FEFB5`。Release 测试 181/181，压缩包未包含个人配置或调试文件。 | Codex |
| 2026-07-25 | 发布 1.3.2：长截图改为真实物理滚轮穿透并加固预览层级、一次右键取消及鼠标事件消费；钉图自动执行本地 OCR，支持选择复制原文及切换在线译文；OCR 结果窗口改进可选择文字、复制与译文显示。安装包 52,297,656 字节，SHA-256 `3596F123B9E32EA91E5067BCD87BB44FEB720578FCF067AF865BCAA773FBAA36`；免安装包 72,019,420 字节，SHA-256 `D1B7439086A183EBB2FD5B3535BEEBDEB83B58F1CD6ACA8D07802F4CBF80EF24`。Release 全量测试 187/187；清单、ZIP 内容、版本号、覆盖更新数据保留和卸载无残留验证通过。 | Codex |

| 2026-07-26 | 修复快速滚动时长截图无法完整拼接：滚轮先验只用于排序而不再否决高置信度图像匹配（容差放宽到半屏/1.5 倍估计，置信度 ≥0.99 时不否决）；时间快速通道的扫描邻域由固定 ±96 行改为 2 倍上一位移（上限 720 行），使加速阶段的大位移仍走低成本通道——在 1200×900 视口上，先验 150 行、真实 420 行的位移由 401 次对齐评分降到 80 次，先验 260 行、真实 700 行由 464 次降到 80 次；行剖面只构建一次供两级搜索复用；采样队列改为“预测式积压”：按滚轮累计位移挑选仍可匹配的最新一帧，匹配失败时按二分回退到更早的中间帧，队列超限时丢弃中间帧并把其滚轮位移并入后继帧，完成时在 450ms 预算内继续拼接积压帧而不再直接丢弃。新增 ScrollFrameSelection 选帧逻辑与 ScrollCaptureFastScrollTests 快速滚动回归用例（大位移匹配、加速甩动序列、先验严重偏小、选帧与合并逻辑）。2026-07-26 已在 Windows 上验证：Release 构建 0 警告 0 错误；快速滚动专项 18/18、全量测试 205/205 通过。 | Claude |
| 2026-07-26 | 根据实测（诊断 scroll-20260726-172933）修复快速反向滚动漏拼：用户下滑后快速上滑，结果图上方内容几乎全丢（上 2 帧、+117px）。根因一，"预测式跳帧"按滚轮增量选帧，而平滑滚动惯性期采样帧滚轮增量为 0，选帧对真实位移失明，直接跳到最新帧导致必败，再以每次约 200ms 的代价二分回退；根因二，锚点一旦落后，此后每帧对旧拼接内容的重叠都不足，桥接失败即清空积压只留最新帧，形成永久失锚循环。将积压处理改为微信式链式顺序拼接：严格按采集顺序逐帧匹配，相邻采样仅相隔数帧间隔、任何速度下都与前帧重叠，锚点全程粘住视口，反向穿过已采集区后自然向上扩展；无法定位的帧丢弃像素但把滚轮位移并入后继帧（积压仅剩一帧时暂存到 CarriedMotion 随下一采样合并），不再整体清空积压；超容量抽帧仍丢中间帧并合并位移。删除 SelectBridgeIndex/SelectFallbackIndex/GetSafeStepRows/GetMaximumMatchableStep 及其 7 个用例；新增"中部起始→下滑→无滚轮证据加速上滑穿过已采集区并越过起点"的逐像素回归用例。Release 构建 0 警告 0 错误；快速滚动专项 12/12、全量测试 199/199 通过；Release 输出已更新（Screenshot.exe 2026-07-26 17:53）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 根据实测（诊断 scroll-20260726-175715）修复慢速滚动停顿再滚动产生的缺段接缝：下行 31 帧仅 1645px、多处 30–70 行内容被跳过后直接粘合。根因一，处理循环把积压全部拼完才回通道取新采样，期间容量 12 的通道持续拒收（本次 1135 次 frame-write-rejected、连续 75 个采样被丢），链条中间环节在入口即丢失，锚点脱节后偶发高置信度误匹配把不相邻内容粘成接缝；根因二，单帧匹配过慢（实测均值 71ms、采样 16ms 追不上）——lockDirection=false 的惯性帧每帧都为反方向空跑一次约 60ms 的全局搜索。修复：处理循环改为每拼一帧就从通道取一次采样，新帧持续进积压、由中间抽帧限量；匹配器新增 preferredNeighborhoodOnly 只跑时间快速通道，反方向探测（种子回退到上次成功步长，邻域天然覆盖反转起步小位移）与两处一致性校验均改用该模式，正向候选置信度加 0.03 超过 1 时按语义等价直接跳过反方向。实测 1039×308 链式步进从 69.9ms 降到 11.0ms（锁定方向 9.1ms），追上采样节奏。保留"滚轮方向滞后仍向上补图"既有回归；新增混合速度（蠕动/停顿/突发交替）逐像素回归用例。Release 构建 0 警告 0 错误；全量测试 200/200 通过；Release 输出已更新（Screenshot.exe 2026-07-26 18:24）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 修复下滑后反向上滑不再扩展（诊断 scroll-20260726-183046 等三次实测）：上行锚点只爬升 1061px（需 3536px）即永久失锚，"上 0 帧"。横向对比三次会话发现"看似正常"的快速下行同样有 131 次链式未命中，只是命中帧步长够大追上了视口；未命中签名为全局搜索满耗时（中位 87ms）且两方向零候选，与正常帧交错出现，而 WPF 同步渲染复现测试全程干净——判定根因是 BitBlt 拷屏与 DWM 合成器竞速产生撕裂帧（上下半屏处于不同滚动位置，双校验带互相矛盾否决全部候选），慢速时撕裂位移小可容忍，快速时未命中连发让视口逃出约 292 行的可匹配范围。修复一：CaptureRegion 拷屏前调用 DwmFlush 对齐合成帧边界，从源头消除撕裂并把采样自然对齐到显示刷新率；修复二：全局搜索在行剖面阶段最佳分数低于接受置信度 0.06 以上时直接放弃，不再为无望帧支付密集/条带校验，未命中成本约 87ms 降至 15ms 内，风暴期恢复速度数倍提升。新增仿编辑器内容（行号列、重复代码块、平滑滚动）真实窗口"下行甩动后反向上行越过起点"集成回归。Release 构建 0 警告 0 错误；全量测试 201/201 通过；Release 输出已更新（Screenshot.exe 2026-07-26 19:12）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 修复"下滑→上滑→再下滑→再上滑"二次反向后顶部段缺失（实测缺 6948–7047）：诊断显示反向过渡帧每帧 98–215ms，处理期间新采样把容量 12 的积压挤爆，抽帧将相邻帧间隙撑到 330–460ms（约 500–800px）远超可匹配上限，链条自锁 11 秒。修复一，积压容量硬顶 12→48（实际由 96MB 内存预算钳制）；修复二，新增采样背压——待拼接帧数超过容量 1/4 时跳过采样节拍，使保留帧间隔收敛为一个处理周期，抽帧在正常场景下不再触发；背压受位移/时间双上限约束（滚轮估计位移≥1/4 视口或 120ms 未采样时强制采帧），保证间隙永不超过可匹配范围。为定位问题给拼接器增加 LastRejectReason/边界漂移诊断字段并写入 JSONL；由此发现两个边界扩展缺陷并修复：长距离行走在周期性内容上累积的锚点坐标漂移会被顶部边界校验以 3 行容差永久拒绝——改为决断置信度（≥0.99）且漂移≤64 行时按边界帧绝对证据重锚归零漂移；快速越界时与边界帧重叠区过窄、稀疏内容纹理不足导致校验探测无结论——改为无反证且主匹配决断时放行。完成排空预算 450→1200ms。新增仿编辑器重复代码内容真实窗口集成回归（下甩→上甩越起点→再下→再上甩），修正其与其它桌面测试的置顶窗口 Z 序竞争。同轮修复实时预览窗越拼越窄：预览位图本为宽度适配+锚点居中裁剪，但窗口高度自适应变矮时 Uniform 缩放按高度挤压宽度——改为 UniformToFill+DownOnly+裁剪，始终填满面板宽度。Release 构建 0 警告 0 错误；全量测试 201/201 通过。 | Claude |
| 2026-07-26 | 内存优化：静态驻留从实测约 358MB 降至托盘静态工作集约 24MB（目标 ≤70MB）。根因是截图历史每条保留全分辨率 BitmapSource（一张长图约 34MB，默认 20 条），且缩略图 TransformedBitmap 链式引用原图。修复：历史条目改为后台把 PNG 写入 `ScreenshotData\HistoryCache` 磁盘缓存（会话级，淘汰即删、启动清扫残留），内存仅保留 WriteableBitmap 物化脱链的小缩略图；查看/复制时从磁盘按需解码。新增 MemoryFootprint.TrimAfterHeavyOperation（Aggressive GC + LOH 压实 + SetProcessWorkingSetSize 归还页面），在普通/滚动/OCR 截图会话收尾、编辑器与预览窗关闭、主窗隐藏到托盘、应用启动就绪时调用；新增 runtimeconfig.template.json 设置 System.GC.ConserveMemory=6。实测：启动即稳 32.7MB，隐藏到托盘后 24.2MB。Release 构建 0 警告 0 错误；全量测试 201/201 通过；Release 输出已更新（Screenshot.exe 2026-07-26 20:14）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 按"任意滚动任意甩动都必须稳"的验收标准继续加固（实测快速摇摆 11 次反转出现 181 次 no-candidate 与 19 次 direction-gap 拒绝）：滚轮方向从"锁定"降为"偏好"——快速反转时滚轮先于平滑滚动翻转，锁定方向会在过渡帧强制搜错方向；现在优先方向全量搜索、反方向始终保留邻域探测（优先候选置信度加 0.03 超过 1 时按语义等价跳过），帧的归属由图像证据裁决。移除已被边界绝对校验取代、且在摇摆中误杀扩展帧的 direction-gap 门。同轮按用户要求把实时预览改为全局整图视图：ComposeLivePreview 不再裁剪锚点切片，始终返回整幅拼接图（超出 1600px 高度预算时保比例整体缩小）；预览窗随图增高（上限为工作区高度，只增不减防抖动），初始仍为选区高度；预览刷新间隔 16→120ms 控制整图渲染的内存带宽。集成回归补充"快速摇摆 4 次反转"阶段，连续 6 轮通过。Release 构建 0 警告 0 错误；全量测试 201/201 通过；Release 输出已更新（Screenshot.exe 2026-07-26 20:28）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 编辑体验升级与紧急修复。表情贴纸从 12 个程序绘制图形升级为 88 个系统原版彩色表情：WPF 文本栈不渲染 COLR 彩色字形，改为自行解析 Segoe UI Emoji 的 COLR/CPAL 分层表并用 GetGlyphOutline 按调色板逐层填色，零打包资源、零第三方依赖、与系统表情观感一致；表情面板改为可滚动网格（EmojiStickerCatalog 目录驱动，灰调表情替换为彩色款以满足颜色校验），贴纸标识由枚举改为 emoji 字符串。箭头改为微信式锥形实心多边形（尾细、杆随线宽渐宽、头部随箭头长度缩放的大实心三角，单一填充形状杜绝杆穿头外凸），预览与成品共用同一几何，两处编辑入口同步生效。紧急修复用户实测"点击箭头卡死闪退"：短拖拽时头长 Math.Clamp 的上限（长度×0.45）低于下限 10 抛 ArgumentException 且未捕获导致整个应用崩溃——改为 Min/Max 组合永不抛出，新增 4 种线宽 × 0–120px 全长度回归；同时增加应用级 DispatcherUnhandledException 兜底（异常写入 ScreenshotData\Diagnostics\crash.log 并提示，驻留进程不再整体闪退）。编辑器加载完成后增加内存修剪，压缩长图编辑态的瞬态解码副本。按用户反馈把箭头头部调小巧（头长改为长度×0.11+线宽×1.6、封顶 44、宽长比 0.36）。修复"已采集条带去重"在极端重复内容上的永久卡死：哈希命中时改为跳过重复条带但锚点继续跟随视口（重复内容依旧不会重复入图；若属采样混叠误判，下一帧自动补拼其余部分），上下两个方向同步修改。Release 构建 0 警告 0 错误；全量测试 202/202 通过（甩动集成回归孤立连跑 6/6）；Release 输出已更新（Screenshot.exe 2026-07-26 21:05）。未上传 GitHub/Gitee，等待用户指令。 | Claude |
| 2026-07-26 | 修复表情在小尺寸面板中的模糊：渲染器改为按显示尺寸精确光栅化（矢量层按需出图，缓存键为表情+尺寸），EmojiStickerImage 请求 2 倍显示尺寸并启用 HighQuality 缩放与布局取整，画布放置的贴纸同样按 2 倍字号出图。甩动集成回归的甩动速度下调约 35%：全套件负载会饿死采样循环（实测仅 104 帧采样），把 CI 门槛测试留在物理余量内，速度极限由孤立压测覆盖。产出软件图标三候选（裁角/取景闪电/光圈，程序绘制含小尺寸预览）与名称提案（简截/闪截/拾光），等待用户选定后再实施品牌替换。Release 构建 0 警告 0 错误；全量测试 203/203 通过。 | Claude |
| 2026-07-26 | 按用户选定实施品牌替换与编辑补全。品牌：名称定为 JianJie（简截的英文形式），新增 AppMetadata.DisplayName 承载显示名——窗口标题、托盘提示、设置页标题、版本页与 csproj Product/AssemblyTitle 全部改为 JianJie，而 AssemblyName/exe 文件名、互斥体、注册表启动项、ScreenshotData 目录保持不变以兼容既有安装与自动更新；README 标题与中英文简介同步改名并把表情描述更新为 88 个系统原版彩色表情。图标：A 案"裁角"（深色圆角底 + 青绿渐变屏幕卡片 + 错位裁角）以程序绘制生成 16–256 十档尺寸 PNG 帧 .ico 覆盖 Assets\Screenshot.ico（路径不变、EXE/窗口/托盘接线自动生效），同步生成 README 用 PNG。编辑补全：详情编辑器（长截图编辑入口）此前没有表情工具入口，补充工具栏"表情"按钮与目录驱动的可滚动表情面板，与遮罩内联编辑器行为一致。Release 构建 0 警告 0 错误；全量测试 202/202 通过；exe 元数据 Product/FileDescription 已验证为 JianJie；Release 输出已更新（Screenshot.exe 2026-07-26 21:27）。 | Claude |
| 2026-07-26 | 用户指出拼音 JianJie 有歧义（易被读作"简介/简洁"），名称最终定为 SnapCut（中文名保留"简截"），并按用户要求执行彻底改名——exe 文件本身改为 SnapCut.exe：AssemblyName Screenshot→SnapCut（产物 SnapCut.exe/dll/pdb 等全套更名），两处 WPF 图标 pack URI 同步改为 /SnapCut;component，DisplayName 与全部界面文本、README、csproj Product/AssemblyTitle 改为 SnapCut；免安装更新器的包内入口校验与重启路径校验改为 SnapCut.exe（重启路径本就取当前进程路径，各版本自洽）；安装脚本 AppName/AppExeName/输出文件名改为 SnapCut 并保留 AppId 以保升级连续性，新增 [InstallDelete] 在升级时清理旧名 Screenshot.exe/dll/pdb/deps/runtimeconfig 残留；构建脚本与更新服务测试同步改名；构建后清理了 Release 输出中的旧名产物。兼容性保留：单实例互斥体与开机启动注册表值名不变（启动项路径由启动同步逻辑自动改写为新 exe），ScreenshotData 数据目录不变，更新清单文件名 Screenshot-Update.json 不变（旧客户端按固定名拉取）。过渡注意事项（发布时处理）：旧版免安装客户端的更新器校验包内必须存在 Screenshot.exe，因此改名后的首个免安装过渡包需同时附带旧名副本或在发布说明中引导手动下载一次；安装版升级由 InstallDelete 平滑处理。Release 构建 0 警告 0 错误；全量测试 202/202 通过；SnapCut.exe 元数据 Product 已验证。 | Claude |
| 2026-07-26 | 用户反馈初版图标在深色任务栏上对比不足、不好看，重制为现代满幅风格并给出三新案（D 蓝紫渐变+白色裁角卡+金色裁片、E 日落渐变+取景闪电、F 双色对切），任务栏深色背景 64/48/32/24/16 小尺寸实测后选定 D 案落地：程序绘制生成 16–256 十档 PNG 帧 .ico 与 README 用 PNG，覆盖 Assets\Screenshot.ico（接线不变），已从构建产物 SnapCut.exe 提取验证嵌入成功并触发系统图标缓存刷新。Release 构建 0 警告 0 错误；全量测试 202/202 通过（期间一次既有滚轮注入测试的环境性超时，重跑即过）。 | Claude |
| 2026-07-26 | 发布 SnapCut 2.0.0（软件由 Screenshot 更名为 SnapCut/简截，启用全新蓝紫渐变裁角图标）。核心变更：长截图拼接内核重写（链式顺序匹配、采样背压+位移/时间双保险、DwmFlush 对齐拷屏、边界绝对证据重锚、决断放行、方向仅作偏好），任意速度与来回滚动稳定拼接；实时预览改为全局整图且预览窗随图增高；后台静态内存约 358MB 降至约 25MB（历史磁盘缓存+会话收尾修剪+GC 保守模式）；表情升级为 88 个系统原版彩色表情（COLR/CPAL 自绘、按尺寸精确渲染、长截图编辑器同步提供）；微信式锥形箭头；应用级异常兜底写入 crash.log。免安装更新器改为校验/重启 SnapCut.exe 并在替换后清理旧名产物；安装器改名并在升级时清理旧文件（AppId 保留）。README 双语更名并注明免安装老用户需手动升级一次。产物：安装包 52,360,558 字节 SHA-256 540B7E94CA5E2DC08EF0A8BF268E4F1C00E6A96AF7927D3C8D7EAC33BA1B7C38；免安装包 72,074,115 字节 SHA-256 F12E20193BB6886DE274158D30A10AF71FA63A3CF007BC2049F8E82B4FA76393；更新清单 updates/Screenshot-Update.json 已更新双源地址（文件名保持不变以兼容旧客户端）。发布前 Release 全量测试 202/202；免安装单文件冒烟启动通过（启动工作集 31.7MB）；ZIP 仅含 SnapCut.exe 无个人数据。代码与 v2.0.0 标签已推送 GitHub 与 Gitee。GitHub Release v2.0.0 已通过 API 创建并上传全部三个附件（安装包、免安装包、Screenshot-Update.json），以客户端视角验证 releases/latest/download 固定地址返回 2.0.0 清单且附件可下载——在线热更新链路即时生效；Gitee 仓库 updates/ 固定清单入口已随代码推送同步为 2.0.0，下载地址在 Gitee Release 附件上传前会自动回退 GitHub 源。Gitee Release 页面附件因本机仅存账号密码（Gitee API 需私人令牌）待用户上传或提供令牌后补传。 | Claude |
| 2026-07-26 | 用户实测 1.3.2 安装版"检查更新"提示"更新清单缺少 Screenshot-Setup-2.0.0-win-x64.exe"，暴露新旧客户端都在 ApplicationUpdateService 中按 Screenshot-Setup/Portable-{版本} 硬构造资产文件名并拒绝其它命名。双管齐下修复：代码改为同时接受 SnapCut- 与 Screenshot- 两种命名（清单声明名必须命中其一且下载地址以其结尾，宿主可信校验不变），新增"重命名后接受 SnapCut 命名清单"的回归用例；本次发布资产改回旧命名 Screenshot-Setup/Portable-2.0.0-win-x64（包内程序仍为 SnapCut）以让全部存量 1.3.2 客户端立即可自动热更新，README 下载链接与发布说明同步并注明命名兼容原因，未来待旧版本用户消化后再切换新命名。重新构建、发布并打包：全量测试 203/203；新安装包 52,386,496 字节 SHA-256 F93A2F599C0A904E8F2F0BD7540DC33FE7ED0BD25BB518846D45A6D88364DCAE；新免安装包 72,074,127 字节 SHA-256 2E41234359FEEAEBC6E2A7FD3C3BAEED40F8DBBD081A2DBF2837BB432BB0090E；清单已更新并随代码推送，GitHub Release v2.0.0 附件替换为新命名文件。 | Claude |
| 2026-07-27 | 开始阶段 16（跨平台核心与 macOS 版），Windows 端源码零改动。16.1 完成：`src/SnapCut.Core` 平台无关拼接内核（PixelImage BGRA 光栅；ImageOverlapMatcher 与 Windows 版逐行同源仅换像素类型；ScrollCaptureComposer/ViewportFingerprint/CapturedFrameGate 的 GDI+ 操作全部改为确定性字节位块传送与盒式缩放；ScrollWheelMotionTracker/ScrollFrameSelection/方向与选项类型平移）。16.2 完成：`tests/SnapCut.Core.Tests`（net8.0 跨平台）移植拼接器 26 用例＋快速滚动 13 用例并新增 PixelImage/帧闸门专项，Release 53/53 一次通过，证明移植忠实。16.3 原型完成：`src/SnapCut.Mac` CLI（纯 C-ABI P/Invoke——CGDisplayCreateImageForRect 抓屏、任意 32 位 CGImage 字节序归一化为 BGRA、ImageIO 写 PNG、听式 CGEventTap 滚轮监听、显示器枚举；滚动采集引擎按 Windows 已验证规则实现链式顺序拼接＋采样背压＋抽帧位移合并＋CarriedMotion；命令 displays/permissions/capture/scroll；缺输入监控权限时降级为纯图像证据）。三工程已入解决方案；全解决方案 Debug 构建 0 警告 0 错误（Release 因用户正在运行 SnapCut.exe 锁定输出而未重建，Windows 代码未变无需重建）；osx-arm64 自包含交叉发布验证通过。后续：16.3 待真实 Mac 验收，16.4 菜单栏 App，16.5 Windows 端择机切换复用 Core（当前算法修改需 Windows/Core 双写，已在分项表标注防漂移）。 | Claude |
| 2026-07-28 | 根据三次实测诊断改造 Windows 长截图交互。日志显示每次会话跳过 2986–4043 次采样、拼接队列最长落后约 4 秒，代码截图在向上越过起点时连续出现 `boundary-unverifiable` / `no-candidate`，随后把累计 288 行滚动量错误压到 33px 位移，造成缺行；根因是人工滚动速度高于全局历史匹配吞吐，不是截图范围本身过大。主流程改为串行受控滚动：单击启动/暂停/继续，固定小步长向下，双击后方向永久锁为向上并返回初始视口，物理滚轮禁用；每帧匹配完成后才滚下一步，从结构上消除队列积压。进度窗按阶段显示操作提示，完整预览继续随结果增长；新路径写入方向、耗时、位移和拒绝原因诊断。Release 隔离构建 0 警告 0 错误；Windows 测试 227/227、跨平台核心测试 53/53 通过。 | Codex |
| 2026-07-28 | 根据受控流程三次实测继续修正：向上返回阶段前 37 帧只经过已采集范围，越过初始位置后连续 34 帧 `no-candidate`，最终错误候选把 231px 上方内容写到结果底部。根因是错误地在初始位置自动收尾并继续复用同一个双向历史锚点。改为双段独立拼接：向下段只扩展底部；返回段按实际向下移动步数快速回程且完全不调用写像素接口；回到或越过初始位置后启用以原始初始帧为锚的独立向上拼接器，降速采集新内容，连续三帧静止确认真实顶部后自动完成；最终以两段共有的完整初始帧确定性重叠合并。向下等待时间 420ms，向上新内容 360ms，快速返回 48ms，并在接近初始位置时提前降速等待画面稳定。新增上下段顺序逐像素回归和返回阶段状态测试；Windows Release 测试 230/230。 | Codex |
| 2026-07-29 | 发布 SnapCut 2.1.0。Windows 长截图改为固定节拍的细粒度滚轮消息，支持单击开始/暂停/继续、双击返回初始位置后反向扩展、鼠标自由移动和真实边界停留；锚点绑定截帧瞬间的输入里程，修复暂停、回程、周期性代码行中的缺行、重影、错误边界及永久校准。长图查看器支持适应窗口、Ctrl+滚轮缩放和无可见手柄拖动。加入跨平台拼接核心、53 项平台无关测试及 macOS CLI 原型。README 中英文功能说明和下载链接同步更新。安装包 52,423,366 字节，SHA-256 `9078F4297290F0A182722BB89434EFFC5C33E0D409BBB06AC2B4CAB434021A6B`；免安装包 72,111,514 字节，SHA-256 `A39B0356DBA46E4A4FEBA68FBF5387D77BED270A97E000AA8E85F13CC56FE1D1`；更新清单同步双源地址。Release 全量测试 378/378，通过版本、清单、ZIP 内容、个人数据排除和免安装包启动冒烟验证。 | Codex |
| 2026-07-29 | 发布 SnapCut 2.2.0。普通截图与长截图编辑器支持直接点击已有矩形、箭头、文字和表情后二次调整位置、大小、端点与边角，矩形仅在精准边界命中，并提供选择、放置、移动、缩放等鼠标指针反馈；设置窗口每次回到前台自动检测 GitHub/Gitee 更新并显示徽标，浅色使用柔和渐变、深色恢复深青亮色渐变；截图工具栏统一为大小协调的镂空矢量图标并保留原有表情图标。长截图边界刷新限制到本帧真实新增条带，保护初始视口和已经拼接内容；固定滚动以已确认运动趋势消除周期代码行短一行误匹配，同时过滤校准帧和重试尖峰，修复暂停、返回后反向扩展、初始接缝中的整行缺失与压缩。安装包 52,379,442 字节，SHA-256 `871328D17145957BCACE26C4D06BB6F8DD29B479E6E347CC683FD0A85FC1DD7D`；免安装包 72,114,201 字节，SHA-256 `E2822F7EA8B54C59488396E3784ACDD4D9D92182730A4D2FF631E043236F3B2E`；更新清单同步双源地址。Release 全量测试 383/383，通过版本、清单、ZIP 内容、个人数据排除和免安装包启动冒烟验证。 | Codex |
| 2026-07-29 | 发布 SnapCut 2.2.1。依据用户问题会话的长截图诊断日志修复两类假匹配：暂停恢复重新锚定时限制相对正常滚动步长的最大可信位移，拒绝异常大跨度扩展；返回初始视口后提高交叉判定置信度，并要求连续两帧方向一致的证据后才开始反向扩展，避免周期性代码行被突然插入、压缩或遗漏。受控诊断新增重叠行数、置信度、横向偏移和预期位移字段，并加入图像级与状态级回归。安装包 52,395,270 字节，SHA-256 `0503575EC861041364A5CC31D7489ED8255BAFF01E9497CBA8BD06102D54DA3E`；免安装包 72,111,406 字节，SHA-256 `BC1FACDDECD67F8C85703B6B362485FB17F15B7C439028199CA889AF238E5E72`；更新清单同步双源地址。Release 全量测试 392/392，通过版本、清单、ZIP 内容、个人数据排除和免安装包启动冒烟验证。 | Codex |
| 2026-07-29 | 发布 SnapCut 2.2.2。修正设置侧栏的新版本提示：检测到更新时把原“版本更新”文字就地替换为亮色“有新版本”，悬停显示目标版本；无更新时恢复原文字和导航颜色。删除在窄侧栏中被裁成单个“有”字的独立徽标，并增加更新/无更新状态、亮色资源、提示文字及侧栏边界回归。安装包 52,420,729 字节，SHA-256 `2AB1E96B838BAB977B841ECEBBC961400F6E8331E34EBAC532420B754F324C70`；免安装包 72,111,281 字节，SHA-256 `C3CA9F4E1C8E86746C321DCA700A39DD7ACEE76F5C4604711C9863D5BC488881`；更新清单同步双源地址。Release 全量测试 393/393，通过版本、清单、ZIP 内容、个人数据排除和免安装包启动冒烟验证。 | Codex |
| 2026-07-30 | 发布 SnapCut 2.3.0。新增在线大模型与 Bergamot/CLD3 完全离线的多语言翻译，自动检测源语言并支持按目标语言下载安装包；设置页新增正式版本历史、发布时间、更新内容和已校验版本更新/回退，每次回到前台重新检查更新。修复免安装版快捷键/主题保存及快捷键录制时触发外部全局动作，完善全局深浅色、窗口位置大小记忆、已有标注二次编辑和长截图接缝稳定性。发布附件正式切换为 `SnapCut-` 前缀，主清单与旧名兼容清单共用同一套安装包和免安装包。安装包 56,548,501 字节，SHA-256 `8FAB2B0D7CB4BA982BE752F2E8F8E963D9DDE4DDDA0A842257169E84C78C0C7B`；免安装包 78,748,640 字节，SHA-256 `1D5FA902FF9D8D6C09E93340DDF5432AC42DC8112EE91E84D69A8206EDB9ABBE`。Release 全量测试 427/427，构建 0 警告 0 错误，通过版本号、双清单一致性、清单大小/哈希、ZIP 内容和免安装包冷启动验证。 | Codex |
