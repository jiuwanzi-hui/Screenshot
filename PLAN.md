# Windows 截图工具执行计划

## 文档说明

本文件是项目的唯一执行计划记录。每完成一个步骤，更新对应状态、完成日期、验证结果和阻塞项。

状态说明：

- 待开始：尚未执行。
- 进行中：当前正在执行。
- 已完成：实现和相应验证均已完成。
- 已阻塞：需要补充需求、权限或外部条件后才能继续。

## 已确定的产品约束

- 目标平台：Windows 10/11。
- 技术栈：C#、.NET 8、WPF 和 Win32 API。
- 优先目标：轻量、后台常驻、截图流程快、界面简洁。
- 程序关闭主窗口后默认继续驻留系统托盘；用户可选择是否在任务栏显示窗口图标。
- 所有应用自身数据（设置、凭据、日志和默认截图）默认保存在可执行文件同级的 `ScreenshotData` 目录；用户手动选择的截图保存位置除外。
- 通知区域图标可由用户完全隐藏；Windows 负责决定已显示的通知区域图标是否进入“隐藏图标”折叠区，应用不尝试干预该系统排序。
- OCR 优先使用本地 Windows 能力；翻译服务必须可配置，不能绑定单一服务商。

## 快捷键冲突规则

1. 本软件的快捷键由用户在设置页配置，并在保存时尝试注册。
2. 若快捷键未冲突，直接保存并生效。
3. 若快捷键被系统保留或 Windows 拒绝注册，说明原因并禁止使用该组合键，不尝试修改系统快捷键。
4. 若快捷键被第三方软件占用，提示冲突并提供处理入口。Windows 没有通用、安全的接口可以直接改写任意第三方软件的快捷键设置，因此只能在用户明确授权且目标软件具备可验证配置方式时进行适配；其他情况要求用户修改本软件快捷键或在目标软件中手动释放该快捷键。
5. 不使用键盘钩子强行拦截系统或第三方应用已注册的快捷键，以避免破坏系统行为和其他软件的输入。

## 执行计划

| 阶段 | 工作项 | 交付与验收标准 | 状态 | 完成日期 | 验证结果 / 阻塞项 |
| --- | --- | --- | --- | --- | --- |
| 0 | 产品边界与架构设计 | 确认模块边界、依赖、数据存储和高风险功能的降级策略 | 已完成 | 2026-07-11 | 已创建 docs/ARCHITECTURE.md；完成架构、数据存储、依赖控制、快捷键边界和高风险降级策略审查。 |
| 1 | 初始化工程 | 可构建、可启动的 .NET 8 WPF 工程，包含基础目录和代码规范 | 已完成 | 2026-07-11 | 已安装 .NET SDK 8.0.422；创建解决方案、WPF 应用和 xUnit 测试项目；dotnet restore、build、test 与 WPF 启动冒烟验证均通过。 |
| 2 | 后台与系统托盘 | 关闭主窗口后可常驻；托盘菜单可打开设置和退出；可配置任务栏图标显示 | 已完成 | 2026-07-11 | 已实现托盘图标、打开设置/退出菜单、关闭窗口后隐藏和显式退出；任务栏显示由窗口接口控制，默认隐藏。构建 0 警告，3/3 测试通过，后台无任务栏窗口启动验证通过。 |
| 3 | 设置中心与配置持久化 | 常规设置、保存路径、托盘/任务栏显示、开机自启和配置文件可正常保存恢复 | 已完成 | 2026-07-11 | 已实现常规设置页、保存目录选择、任务栏图标开关和开机启动开关；配置、凭据和默认截图统一写入可执行文件同级 `ScreenshotData`，启动项使用当前用户注册表。构建 0 警告，相关测试通过。 |
| 4 | 全局快捷键 | 可配置截图、长截图、OCR、贴图和设置快捷键；按上述冲突规则处理 | 已完成 | 2026-07-11 | 已实现可编辑的 5 组全局快捷键、格式解析、重复检测、已知系统保留键拒绝和 RegisterHotKey 原子注册/回滚；第三方占用时明确提示用户释放或换键，不强行拦截。构建 0 警告，12/12 测试通过，罕见组合键真实注册/释放验证通过。 |
| 5 | 基础区域截图 | 多显示器透明遮罩、框选、Esc 取消、截图预览均可用 | 已完成 | 2026-07-11 | 已实现基于虚拟桌面的透明框选遮罩、Esc 取消、物理像素区域换算、实际屏幕抓取和预览窗口；应用声明 PerMonitorV2 DPI 感知。构建 0 警告，15/15 测试通过，真实屏幕像素抓取和预览位图验证通过。 |
| 6 | 截图结果处理 | 支持复制到剪贴板、保存到指定路径、重新框选和基础历史记录 | 已完成 | 2026-07-11 | 已在预览中实现复制到系统剪贴板、保存 PNG、重新框选；保存采用 CreateNew 防止覆盖同名文件。新增可选的会话内缩略图历史和托盘入口。构建 0 警告，17/17 测试通过，真实 PNG 保存和历史容量验证通过。 |
| 7 | 图片编辑器 | 支持文字、箭头、矩形、画笔、马赛克、撤销和重做 | 已完成 | 2026-07-11 | 已实现独立编辑器：矩形、箭头、画笔、文字输入、马赛克、颜色/线宽、撤销/重做，以及编辑结果复制和 PNG 保存。编辑注释以命令模型管理，原图不被破坏。构建 0 警告，20/20 测试通过，马赛克、命令栈和编辑器窗口验证通过。 |
| 8 | 钉图 | 截图可作为无边框置顶图窗显示，支持拖动、缩放、透明度、复制和关闭 | 已完成 | 2026-07-11 | 已实现独立无边框置顶钉图窗口，可拖动、右下角缩放、调节透明度、复制和关闭；钉图不显示任务栏图标，管理器在应用退出时统一释放。构建 0 警告，22/22 测试通过，置顶和无任务栏属性验证通过。 |
| 9 | OCR 识别 | 可识别截图或选区文字，支持复制结果和识别语言设置 | 已完成 | 2026-07-11 | 已接入 Windows 本地 OCR：支持从截图预览或独立选区识别，结果窗口可编辑和复制；设置页可配置 OCR 语言标记，并对缺失语言包给出明确提示。项目目标为 net8.0-windows10.0.19041.0。构建 0 警告，25/25 测试通过，语言枚举和缺失语言包降级验证通过。 |
| 10 | 翻译 | 可翻译 OCR 文本；支持翻译服务配置、网络失败提示和结果复制 | 已完成 | 2026-07-11 | 已实现 OCR 结果的显式翻译按钮和 OpenAI 兼容 HTTPS 接口配置。默认不发送任何文字；需用户开启在线发送并点击翻译。API Key 通过当前用户 DPAPI 加密保存，不进入 settings.json。已处理无配置、无凭据、HTTP、鉴权、网络和响应格式错误。构建 0 警告，29/29 测试通过，所有协议测试使用本地模拟响应。 |
| 11 | 长截图 | 覆盖浏览器和常规可滚动窗口；支持拼接、取消和失败降级 | 进行中 |  | 已实现框选滚动区域后恢复原窗口焦点，由用户滚动鼠标；程序以 40ms 间隔采样，最后一个滚轮事件后继续采样 360ms，覆盖浏览器平滑滚动尾段。拼接器以压缩的纹理视口锚点和双向位移匹配定位当前帧：反向滚回已采集范围只刷新锚点，越过顶部或底部才添加条带，即使滚轮方向滞后也不会漏掉上方内容。匹配固定中央内容带并忽略常见顶部栏；比较时以文字、头像和图片等高纹理区域加权，纯色背景不再主导置信度。待拼接帧还会与最近 4 个已确认视口交叉验证，逻辑位移矛盾即拒绝，默认置信度提高至 0.96，避免聊天记录等稀疏布局的重复和缺段。滚动期间保留点击穿透的选区边框，右侧实时预览持续更新且两者在取帧时暂时隐藏，避免进入结果图。Release 长截图专项测试 14/14、框选遮罩专项测试 7/7、全量测试 69/69 通过；等待本轮 Release 的浏览器和实际软件人工验收。 |
| 12 | 开机启动与系统集成 | 可开启/关闭开机启动，状态与 Windows 启动项实际状态一致 | 已完成 | 2026-07-11 | 已使用当前用户 `Run` 注册表项启用/关闭开机启动；启动命令带 `--background`，开机时静默驻留，手动启动仍显示设置窗口。启动项测试 2/2、全量测试 50/50 通过。 |
| 13 | 性能与稳定性 | 验证后台内存、资源释放、热键恢复、异常日志和多显示器兼容性 | 待开始 |  |  |
| 14 | 界面打磨与发布 | 完成简洁界面、深浅色支持、安装包、版本信息和使用说明 | 进行中 |  | 已完成 1.0.0 自包含 x64 安装包与免安装包、动态深浅色中文安装向导、自定义安装位置、版本信息与中英文 README；待后续正式发布签名。 |
| 15 | 微信/PixPin 工作流重构 | 设置即时生效、按键录入快捷键、便携数据目录、可隐藏通知区图标，以及框选后留在屏幕内调整和编辑 | 进行中 |  | 15.1 至 15.3 已完成；15.3 已补充右键逐层返回；15.4 已完成本轮边界去重、反向回滚去重、动态视口去重、方向滞后补偿、选区常显和右侧实时预览的实现与自动验证，等待用户用本轮 Release 完成实际滚动页面验收。 |

### 阶段 15 分项

| 分项 | 工作项 | 状态 | 验收标准 / 验证结果 |
| --- | --- | --- | --- |
| 15.1 | 便携存储与通知区图标 | 已完成 | 设置、凭据和默认截图已改为可执行文件同级 `ScreenshotData`；新增“显示通知区域图标”开关，关闭后完全隐藏该图标。Release 构建 0 警告、0 错误；4/4 存储与设置测试通过。Windows 不允许应用指定图标位于主托盘还是隐藏图标折叠区。 |
| 15.2 | 即时设置与快捷键录入 | 已完成 | 已移除手动保存按钮；勾选项立即生效，文本项停止输入 400ms 或离焦后生效，快捷键框直接捕获键盘组合并即时注册。每项快捷键可通过清除按钮或 `Backspace/Delete` 留空，清空后立即注销对应全局热键；无效、系统保留或冲突组合会回滚并显示原因。Release 专项测试 16/16 通过。 |
| 15.3 | 微信式框选后流程 | 已完成 | 框选后保留屏幕遮罩与选区，支持拖动和八方向缩放；点击编辑后在原选区冻结画面并直接使用矩形、箭头、画笔、文字、马赛克、颜色、线宽、撤销和重做，不再打开独立编辑窗口。鼠标右键按层级处理：取消正在绘制／输入的标注、退出编辑回到选区、清除选区，最后取消截图。工具栏对号会复制当前编辑结果并关闭遮罩，同时保留保存、OCR、钉图和取消。编辑与遮罩专项测试 10/10 通过。 |
| 15.4 | PixPin 式长截图 | 已完成 | 已实现框选滚动区域、恢复目标焦点、用户鼠标滚动、持续采集、固定区域容错、可靠帧筛选、可见帧数、“完成/取消”、低内存条带拼接和失败降级；在最后一个滚轮事件后继续采样 360ms，覆盖平滑滚动剩余位移。拼接器追踪当前视口的逻辑坐标，并保存至多 128 个压缩纹理指纹；每帧同时校验滚动方向与反向位移，方向事件滞后时依然选择图像证据更强的一侧。回到已采集视口只恢复锚点，只有越过现有顶部或底部才追加新条带。匹配始终使用排除顶部栏的中央内容带，并以文字、头像、图片和左侧行号等局部纹理加权，避免纯色聊天背景放大错误匹配；较窄窗口保留完整区域采样，宽窗口提高横向采样密度。默认匹配置信度提高至 0.96。框选边框在屏幕上持续可见且点击穿透，实时预览优先显示在选区右侧；两者均在取帧时隐藏以保证输出干净。预览改为对完成后的完整拼接图只缩放一次，避免分段缩放产生条纹。Release 长截图专项测试 16/16、框选遮罩专项测试 7/7、全量测试 71/71 通过。 |

## 高风险功能与策略

| 功能 | 风险 | 首版策略 |
| --- | --- | --- |
| 长截图 | 应用程序的滚动机制差异大，自动拼接容易错位 | 先支持浏览器和常规滚动窗口；不能可靠识别时提示降级为普通截图 |
| 第三方快捷键冲突 | Windows 不能识别并通用修改占用方的配置 | 只检测并提示；仅对明确支持的应用提供用户授权后的专门适配 |
| OCR 与翻译 | 离线能力、语言包和网络服务可用性不同 | OCR 优先本地；翻译服务可配置并提供清晰的失败状态 |
| 后台常驻 | 资源泄漏会长期影响系统 | 统一管理托盘、截图资源、置顶窗口和全局快捷键的生命周期 |

## 更新记录

| 日期 | 更新内容 | 执行人 |
| --- | --- | --- |
| 2026-07-11 | 创建初始计划；补充快捷键冲突和系统保留快捷键处理规则 | Codex |
| 2026-07-11 | 完成阶段 0：写入架构设计并完成文档验收 | Codex |
| 2026-07-11 | 完成阶段 1：初始化 .NET 8 WPF 工程、测试工程和构建规范；构建、测试、启动验证通过 | Codex |
| 2026-07-11 | 完成阶段 2：实现后台常驻、系统托盘和窗口隐藏生命周期；构建、测试和后台启动验证通过 | Codex |
| 2026-07-11 | 完成阶段 3：实现设置中心、配置持久化和当前用户开机启动；构建和 7 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 4：实现全局快捷键配置、系统保留键处理和 Windows 注册验证；构建和 12 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 5：实现区域截图、虚拟桌面坐标换算和截图预览；构建和 15 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 6：实现截图复制、保存、重选和会话内历史；构建和 17 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 7：实现图片编辑器、马赛克和撤销/重做；构建和 20 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 8：实现钉图置顶窗口和生命周期管理；构建和 22 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 9：实现本地 Windows OCR、结果复制和语言设置；构建和 25 项功能测试通过 | Codex |
| 2026-07-11 | 完成阶段 10：实现可选在线翻译、DPAPI 凭据保护和网络失败处理；构建和 29 项功能测试通过 | Codex |
| 2026-07-11 | 开始阶段 11：长截图进入实现与验证，计划表状态更新为进行中 | Codex |
| 2026-07-11 | 根据交互反馈新增阶段 15：微信/PixPin 工作流重构；15.1 便携存储与通知区图标开始实现 | Codex |
| 2026-07-11 | 完成阶段 15.1：便携数据目录和通知区域图标开关；Release 构建及 4 项相关测试通过 | Codex |
| 2026-07-11 | 完成阶段 15.2：即时设置、键盘快捷键录入、冲突回滚与设置窗口验证；Release 构建及 12 项相关测试通过 | Codex |
| 2026-07-11 | 完成阶段 15.3：微信式框选后调整、工具条与操作流程；Release 构建、2 项选区窗口测试及 41 项非长截图回归测试通过 | Codex |
| 2026-07-11 | 完成阶段 12：开机启动使用 `--background` 静默驻留；启动项测试 2/2、全量测试 50/50 通过 | Codex |
| 2026-07-11 | 阶段 11/15.4 实现和自动验证完成：长截图改为框选后由用户滚动、点击完成再拼接；专项测试 6/6、Release 全量测试 50/50 通过，进入实际软件人工验收 | Codex |
| 2026-07-11 | 补充阶段 15.2：允许任意快捷键留空并立即注销对应注册，增加清除按钮和 `Backspace/Delete` 操作；专项测试 16/16 通过 | Codex |
| 2026-07-11 | 重做阶段 11/15.4 长截图匹配：恢复目标窗口焦点、提高采样频率、容忍固定顶部和滚动条、后台匹配并改为低内存条带存储；专项测试 8/8 通过 | Codex |
| 2026-07-11 | 重做阶段 15.3 编辑流程：编辑画布嵌入原截图选区，支持原有五类工具与撤销/重做；增加对号复制并关闭。专项测试 8/8，实际界面框选、绘制和关闭验证通过 | Codex |
| 2026-07-12 | 修复阶段 11/15.4：在重叠匹配前拒绝到顶/到底的零位移帧，避免周期性内容重复拼接；长截图时保留点击穿透的选区边框，并在右侧持续显示实时预览，取帧时暂隐二者以确保输出不受 UI 污染。Release 长截图专项测试 10/10、框选遮罩专项测试 5/5、全量测试 61/61 通过；Release 构建 0 警告、0 错误。桌面交互测试已串行化，避免全局滚轮、前台窗口和剪贴板互相干扰。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 反向回滚重复拼接：拼接器改为记录当前视口和已采集范围的逻辑坐标；视口回到既有范围只更新锚帧，越过顶部或底部才保存新增条带。新增“下滑后上滑回原区域再继续下滑”回归测试；将 WPF 集成测试改为注入滚轮事件，避免受用户桌面前台窗口影响。隔离 Release 构建 0 警告、0 错误；长截图专项测试 11/11、框选遮罩专项测试 5/5、全量测试 62/62 通过。 | Codex |
| 2026-07-12 | 加固阶段 11/15.4 重复检测：针对卡片列表等周期性布局在末端因悬停、动画或滚动条轻微变化而误判新条带的问题，新增压缩视口指纹历史；命中既有视口时恢复该位置且不拼接。新增“轻微动态变化的重复末端帧”回归测试。隔离 Release 构建 0 警告、0 错误；长截图专项测试 12/12、框选遮罩专项测试 5/5、全量测试 63/63 通过。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 反向滚动漏段与接缝错位：实现 48×36 纹理视口锚点、双向图像位移校验和方向滞后补偿；将匹配区域固定为排除常见顶部栏的中央内容带，避免卡片网格的局部重复误导整帧；最后一个滚轮事件后继续采样 360ms，覆盖平滑滚动尾段。新增“方向滞后仍向上补图”“小范围动态变化后回滚”和“下→上→下逐像素连续”回归测试。Release 长截图专项测试 13/13、框选遮罩专项测试 5/5、全量测试 66/66 通过。 | Codex |
| 2026-07-12 | 补充阶段 15.3 鼠标右键逐层返回：预备状态直接取消；有选区时清除选区；编辑时先取消未完成的绘制或文字输入，再退出编辑回到选区。新增“选区→取消”和“编辑→选区→取消”遮罩回归测试。Release 框选遮罩专项测试 7/7、全量测试 68/68 通过。 | Codex |
| 2026-07-12 | 修复阶段 11/15.4 稀疏聊天记录误拼：重叠比较改为按局部纹理加权，文字、头像和图片主导位移判断，纯色背景不再提高错误候选的置信度；待拼接帧与最近 4 个已确认视口交叉验证，坐标不一致即拒绝；默认重叠置信度提高至 0.96。新增“稀疏聊天气泡、动态消息、逐像素连续”回归测试。Release 长截图专项测试 14/14、框选遮罩专项测试 7/7、全量测试 69/69 通过。 | Codex |
| 2026-07-13 | 修复阶段 11/15.4 重复拼接与预览条纹：恢复左侧行号/头像锚点，按窗口宽度提高横向采样密度并保留窄窗口滚动条容错；拼接预览改为先完成整图再单次缩放；新增重复代码行号对齐和一次性预览缩放回归测试。Release 长截图专项测试 16/16、框选遮罩专项测试 7/7、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 完成长截图结果交互：滚动完成后的预览定位在框选区域右侧并锁定选区；新增对号按钮，点击后复制图片并关闭预览；编辑按钮进入详情编辑器，保留保存、钉图和 OCR 操作。Release 全量测试 71/71、构建 0 警告 0 错误。 | Codex |
| 2026-07-13 | 更新应用视觉标识：新增高对比截图取景框图标，扩大主体占比以适配任务栏小尺寸；接入 EXE、WPF 窗口和系统托盘。Release 构建 0 警告 0 错误。 | Codex |
| 2026-07-13 | 修复长截图缺段与交互状态：重叠候选改为采用置信度最高的真实位移，不再偏向较小位移；采样间隔缩短至 20ms，默认接受帧上限提高至 600，达到上限后不再自动结束而是等待用户操作。框选完成后先保留移动和八方向缩放，点击确认开始滚动后才锁定；右侧实时预览直接提供对号和编辑，对号生成后复制并关闭，编辑生成后进入详情编辑器。Release 长截图专项 16/16、框选遮罩专项 7/7、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 重构长截图首帧交互：框选完成即显示右侧首帧预览，移动和八方向缩放会同步更新选区、滚轮监听区域与预览；第一个滚轮消息分发前同步锁定选区并切换点击穿透，底层窗口从第一下开始滚动。普通截图在遮罩显示前冻结屏幕，编辑直接裁剪冻结帧，消除隐藏/恢复遮罩的闪屏；编辑画布图片改为填充像素画布，修复高 DPI 下二次缩放形成的小块。Release 相关专项 26/26、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复遮罩与边界滚动：滚轮前保留手柄可调和遮罩拦截，滚轮后仅选区内部点击穿透，遮罩外继续阻止其它应用操作；移除选区旁重复取消按钮。新增白色空白帧过滤，避免滚动到底后继续滚动拼接白块；保留视口指纹和双向坐标校验，降低下滚到底再上滚时的重复/错位。Release 相关专项 23/23、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复翻译 API Key 输入后消失：保存设置后保留 PasswordBox 的掩码内容，并跟踪 Key 是否被修改；非空 Key 加密保存，清空输入时删除对应凭据，不再因自动保存清空界面。Release 全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 完善 OCR 与大模型翻译：OCR 输入在尺寸上限内自动放大至最多 2 倍，改善小字号截图识别；新增可配置翻译模型，DeepSeek 域名自动将默认模型映射为 `deepseek-chat`，兼容其它 OpenAI 格式模型。翻译错误继续显示 HTTP 状态便于定位。Release 翻译/OCR 专项 6/6、全量测试 71/71 通过。 | Codex |
| 2026-07-13 | 修复大模型翻译配置持久化和 404：启动时从 DPAPI 凭据库重新加载 API Key 并显示掩码；设置页正确显示模型名称；DeepSeek/OpenAI 根地址自动补全聊天完成接口路径；OCR 结果窗口点击翻译时读取最新设置，不再保留配置前的旧快照。新增 DeepSeek 根地址与 `deepseek-chat` 请求回归测试。Release 翻译/OCR/设置专项 8/8、全量测试 72/72 通过。 | Codex |
| 2026-07-13 | 修复长图编辑白边和滚动闪烁：详情编辑器图片改为填满原始像素画布，消除高 DPI 下上下大块留白；采集帧时只隐藏选区边框，四周遮罩持续显示，不再逐帧闪烁。第一次滚动后拦截鼠标点击、侧键、水平滚轮和全部键盘输入，只允许选区内垂直滚动及右侧预览操作，避免切换应用或修改后方内容。Release 相关专项 26/26、全量测试 72/72 通过。 | Codex |
| 2026-07-13 | 紧急修复长截图输入锁死：移除全局键盘封锁，避免系统快捷键和其它窗口全部失效；鼠标限制增加右侧预览白名单，取消、编辑、对号和窗口关闭始终可点击，退出长截图后立即卸载钩子。Release 相关专项 25/25、全量测试 72/72 通过。 | Codex |
| 2026-07-15 | 根据 EV 操作录像与微信/本软件结果图修复长截图：滚动后改用绘制在选区外侧的独立轮廓，采集时不再隐藏/恢复边框，彻底消除框闪烁；视口指纹恢复左边缘行号/头像并缩小右侧忽略区，粗匹配增加横向采样和候选数；零位移边界置信差从 0.025 收紧至 0.006，避免真实滚动被判为静止。测试改用真实编辑器式固定深色边栏，覆盖滚到底再回滚不重复中间代码行。Release 长截图/遮罩专项 25/25、全量测试 80/80 通过。 | Codex |
| 2026-07-15 | 加固重复代码拼接与遮罩可见性：匹配增加左侧稀疏锚点独立证据，主体负责大致位移，行号/头像负责消除重复内容歧义；只有左侧纹理明显稀疏时启用加权，避免影响浏览器随机内容和固定头部。新增非整行滚动距离的下→上→下连续性测试。四周遮罩提高不透明度，滚动后外置轮廓加粗且全程保持显示。Release 长截图专项 18/18、长截图/遮罩专项 26/26、全量测试 81/81 通过。 | Codex |
| 2026-07-14 | 修复翻译/DeepSeek API Key 不生效：翻译服务类型不再依赖用户手填魔法字符串，空值或大小写不一致统一解析为唯一支持的 OpenAI 兼容类型；保存/读取凭据改用解析后的稳定 provider id，不再因服务类型为空触发异常导致 Key 存不进去。`TranslationProvider` 默认值改为 `OpenAICompatible`，设置页提示补充 DeepSeek 地址与模型示例。新增空/异常大小写解析与凭据键回归测试。 | Claude |
| 2026-07-14 | 补充微信/PixPin 式区域截图交互：选区内双击直接确认（复制并完成），与 Enter、工具栏对号共用同一确认逻辑；OCR 结果窗口在已开启在线翻译且识别出文字时自动翻译一次，结果窗口仍保留供手动操作。Release 构建 0 警告 0 错误，全量测试 79/79 通过。 | Claude |
| 2026-07-15 | 防止长截图遮罩再次造成全局鼠标锁死：低级鼠标钩子仅在长截图选区内部拦截点击、侧键和水平滚轮，选区外交由全屏遮罩处理；右侧预览仍保留点击白名单，键盘输入不做全局拦截，钩子在完成、取消、异常和窗口关闭后释放。新增鼠标消息分类回归测试。Release 遮罩/输入专项 37/37、全量测试 92/92 通过。 | Codex |
| 2026-07-15 | 修复滚动长截图界面缺失：发布选区时立即计算四周遮罩和滚动轮廓尺寸，避免锁定后出现 0×0 的不可见框选区域；右侧实时预览窗口在显示和选区变化后显式提升到遮罩之上，同时保持不激活、不抢目标窗口焦点。新增轮廓尺寸回归断言。 | Codex |
| 2026-07-15 | 根据《本软件长截图视频》修复右键与长截图状态返回：选区发布后第一次右键清空选区并允许重新框选，第二次右键取消整个截图；滚动锁定后选区内右键由低级鼠标钩子转为专用取消信号并吞掉，避免触发后方应用，选区外遮罩和右侧预览上的右键也能取消。取消后统一释放滚轮钩子、关闭预览和遮罩。新增逐级右键回归测试；Release 全量测试 93/93 通过。 | Codex |
| 2026-07-15 | 消除第一下滚轮后的输入竞态：滚轮钩子检测到首个有效滚轮消息时立即开启非滚轮鼠标拦截，保证从滚动开始右键取消不会穿透到目标窗口。Release 长截图/遮罩专项 38/38、全量测试 93/93、构建 0 警告 0 错误。 | Codex |
| 2026-07-15 | 根据 EV 录屏再次修复长截图可见性：移除遮罩与右侧预览窗口的永久 `WDA_EXCLUDEFROMCAPTURE`，改为仅在 `CopyFromScreen` 读取目标帧的瞬间启用捕获排除，读取后立即恢复并重新提升预览层级；删除每帧 16ms 的排除等待，避免录屏中持续消失或形成闪烁。滚动轮廓提升绘制层级并加粗，新增取帧排除/恢复成对回归断言。Release 相关专项 21/21、全量测试 93/93 通过。 | Codex |
| 2026-07-15 | 修正上一版逐帧切换造成的强烈闪烁：彻底移除滚动采样循环中的显示亲和性切换与重复置顶，遮罩、外置边框和预览在滚动期间不再改变窗口合成状态；边框完全绘制在选区外，预览强制位于选区外，保持输出干净。预览尺寸按实际 DPI 换算，并按选区所在显示器工作区定位：优先紧贴右侧，右侧剩余 200–299px 时自动缩窄，连操作按钮都放不下时才紧贴左侧，不再退到屏幕左上角。新增自适应宽度与左右侧贴边定位测试。Release 全量测试 96/96、构建 0 警告 0 错误。 | Codex |
| 2026-07-20 | 滚动拼接升级为微信式多阶段匹配：行剖面粗相关覆盖全部位移、密集像素复核峰值、连续滚动时间先验抑制周期内容误匹配、近置信度并列优先更小连续步长；采样 16ms 主频 + 滚轮静止后 24ms 稳定帧、惯性尾窗 420ms。新增周期内容时间先验与多小步连续滚动无缝拼接回归。Release 全量测试 98/98 通过。 | Codex |
| 2026-07-20 | 继续逼近微信长截图：自适应时间先验邻域（8–64px，随上次步长放大）、峰值±3 局部精修、弱峰值过渡帧丢弃；下行拼接增量复合缓存 + 预览直缩放避免每帧整图克隆；新增大步跳变与微信式气泡会话拼接回归。Release 长截图专项 38/38、全量 100/100 通过。 | Codex |
| 2026-07-20 | 补全条带模板匹配与水平微对齐；修复粘性页头场景下 full/lower 频带混合把正确匹配置信度压到 0.99 以下的问题；密集阶段跳过条带证据、时间先验强时跳过远距 lattice 以保跟手；成功对齐即更新位移先验。Release 全量 100/100。 | Codex |
| 2026-07-23 | 新增关闭按钮最小化/退出设置；完成 1.0.0 自包含 x64 安装包，支持动态深浅色中文向导、自定义安装位置、当前用户/所有用户安装、快捷方式和标准卸载；安装版数据迁至 LocalAppData，便携版策略保持不变。补充中英文 README 与隐私排除规则。Release 全量测试 131/131，安装、启动、卸载验证通过。 | Codex |
