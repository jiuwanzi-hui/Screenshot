# Screenshot 项目习惯

软件的宗旨是：在保证功能的情况下，尽量占用最少的资源。

这份文件记录本项目中可复用的任务约定。执行具体任务时，先读取并遵守本文件；用户在当前对话中的明确要求优先于这里的默认约定。

## 版本发布

- 正式发布版本不得包含调试、输入追踪或其他杂项日志记录；只保留产品明确需要的错误诊断，并确保日志不会在全局输入钩子或界面线程中同步写盘。
- 当用户说“发布新版本”但没有指定版本号时，先检查最近提交、变更范围和现有版本记录，再按语义化版本判断：
  - 仅修复问题、兼容性修正或小幅内部改动：递增 patch（例如 `3.2.0` -> `3.2.1`）。
  - 新增向后兼容的功能或较大的用户可见改动：递增 minor（例如 `3.2.0` -> `3.3.0`）。
  - 存在不兼容变更或明确的大版本重构：递增 major（例如 `3.2.0` -> `4.0.0`）。
- 如果用户明确指定版本号，始终使用用户指定的版本号，不自行改动；若版本号与仓库现状冲突，先说明冲突并核对后再发布。
- 发布新版本时，默认完成以下内容：
  1. 检查并总结实际变更，更新版本号、README 和发布说明。
  2. 构建并验证 Windows 发布产物。
  3. 提交最新代码、创建版本标签，并推送 GitHub 和 Gitee。
  4. 创建 GitHub Release，并上传 Windows 安装包、便携包和更新清单。
- Gitee 发行版由用户手动发布：使用已登录的内置浏览器打开新建发行版页面，向用户提供各字段应填写的内容和全部附件路径，但不代替用户上传附件或点击发布。除非用户在当前任务中明确要求改为自动操作，否则始终遵守此分工。
- GitHub 发行版由助手完成创建和附件上传；上传前检查附件是否齐全。
- 发布完成后，分别核对 GitHub 和 Gitee 的标签、发行版页面、发布说明和附件是否实际可访问，不以本地命令成功作为唯一依据。
- 正式发布属于外部状态变更；如果当前工具或安全策略要求最终确认，在最后创建发行版前向用户确认一次。

## 开发范围与平台

- 用户没有提及 macOS 时，默认只修改和验证 Windows 功能。
- 不要为了完成 Windows 任务提交、覆盖、回滚或清理 macOS 文件；工作区中已有的 macOS 修改和未跟踪文件必须保留。
- 用户明确要求 macOS 时，才把 macOS 代码、工程文件、测试或发布产物纳入任务范围。
- 除非用户在当前任务中明确要求，不得修改长截图的逻辑或算法代码。

## Windows 开发验证

- 功能修改完成后先检查是否有正在运行的 SnapCut/Screenshot 程序；如有，自动关闭它，避免文件锁和旧进程干扰验证。遇到以提升权限运行的实例时，直接使用管理员权限关闭，无需在对话中再次确认。
- 使用 Release 配置编译 Windows 版本，并在以下目录验证最新编译结果：
  `D:\work\code\Screenshot\src\Screenshot.App\bin\Release\net8.0-windows10.0.19041.0`
- 验证完成后打开该目录中的最新程序，让用户可以直接检查当前构建。
- 上述关闭、编译和启动验证流程属于本项目默认操作，无需重复征得用户授权。
- 按改动风险运行相关测试；报告测试结果时明确区分代码失败、环境限制和依赖真实桌面输入的集成测试失败。

## 话术扩展

用户后续补充的固定话术、操作习惯或交付要求，追加到本文件对应章节；若没有合适章节，再新增简短章节。新增约定不得覆盖用户在当前对话中的明确指令。
